# AGENTS.md

## PUSH LOG (IMPORTANT)
Keep **PUSH_LOG.md** up to date on EVERY push. Whenever anyone (you or a collaborator) pushes to the remote, add a new entry at the BOTTOM of `PUSH_LOG.md`: a new date heading, the **Philippine time (PHT, UTC+8)** of the push, whose push it was, and a short summary of what that push did. New entries go below the previous date's entry so the log reads oldest â†’ newest. (Local-only merges are NOT logged.) **Entry FORMAT (as of 2026-09-01):** write each change as its own bullet under the heading — `- Change title: brief description of the function/UI change` (e.g. `- Fixed collapsed-sidebar hover labels: ...`). Keep bullets self-contained and readable without code — describe WHAT changed and WHY (the function or UI behavior), not code snippets. One bullet per distinct change; the first bullet always summarizes the push. **Include the `PUSH_LOG.md` entry IN THE SAME COMMIT as the changes it describes — one commit per push (no separate log-only commits), and keep entries concise but detailed on what changed. This applies to ALL collaborators.** **STYLE PREFERENCE (as of 2026-09-08):** the user wants the change log written in the simple, scannable style used for the daily "Today's Changes" PDF list — a SHORT action-first title (e.g. `Implemented Locking Mechanism`, `Simplified the down-payment tier`) followed by a brief one-line description of WHAT changed and WHY. Keep it short and easy to scan; avoid long multi-clause paragraphs and code-level detail. **INCLUDE the date (and time when known) of the changes** in the heading/entry so it is easy to see WHEN each change was made.

## Project State Summary
React 18 + Vite + TS frontend prototype ("Docufy PSMS"). No backend; mock/localStorage data. Roles: customer, staff, admin.

### Works
- ORDER TOTAL MISMATCH FIX + PER-TAB LOGIN (2026-09-11 5:58 PM PHT): two prototype fixes. **(1) Staff/admin order total now matches what the customer was charged**: customer and walk-in orders were being created without a `costBreakdown` (printing cost + add-ons + total). The staff/admin Orders list (`UnifiedOrders.tsx`) falls back to a legacy flat per-page estimate when no breakdown is stored, producing a different total than the matrix pricing the customer actually paid. Fixed by attaching the exact `costBreakdown` (files total + add-ons + grand total) to every new order at creation time in `PrintTransaction.tsx` (both customer `handleSubmit` and walk-in `handleProceedToQueue`). Existing historical orders without a stored breakdown still display the old fallback estimate. **(2) Per-tab login sessions** (prototype-only convenience): the logged-in user is now stored in the tab's own `sessionStorage` (not shared `localStorage`), so one tab can stay logged in as customer while a separate tab is staff/admin. Refreshing a tab no longer replaces its user with whoever is logged in on another tab.
- CUSTOMER PRINT REQUEST LOCKED WHILE PAUSED/CLOSED + DOWN PAYMENT = CASH-ON-PICKUP ONLY (Form at the print-request checkout + down-payment flow, 2026-09-11): two customer-checkout changes. **(1) Print Request fully locked during outages**: previously the customer Print Request page only disabled the final Place Order button with a small inline notice — customers could still walk through upload/options and hit the disabled button at Step 4. Now, when the shop is Paused or on Scheduled Close (admin Shop Status toggle → `shopStatusStore`), the whole customer print-request form is replaced by a lock screen (`PrintTransaction.tsx`, customer mode) — an amber (paused) or gray (scheduled close) icon + "Docufy is currently paused" / "Docufy is on scheduled close" heading, the admin-stored reason + estimated return when present, a "New print requests can't be placed right now — existing orders are safe and will resume once we reopen." message mirroring `ShopStatusBanner.tsx`, and a "Back to Dashboard" button. Walk-in mode is unchanged (already gated at the submit button). Live status subscription re-renders the lock instantly when the toggle flips. **(2) Down payment applies ONLY to Cash on Pickup**: the 50%-down function (the `/customer/payment-method/:orderId` page) is now reachable only when the customer selects Cash on Pickup. Online-method payments on a down-payment-tier order (₱50–99) no longer get a down-payment step — they're held and routed straight to payment verification and must be paid in FULL online. In `PrintTransaction.tsx`, `requiresDownPayment = flowTier === "down" && !isOnline` and `requiresFullPayment = isFullTier || (flowTier === "down" && isOnline)` (both module-level flags and the `handleSubmit` local re-derivation), the down-payment route is `!isOnline && flowTier === "down"`, "Proceed to Down Payment Method" shows only for cash, Step-4 online note + Place-Order/Go-to-Payment-Verification confirm dialog lost their down-payment copy (plain online wording), and the success modal's payment-method nav is cash-gated too. `DownPaymentMethod.tsx` (the down-payment page) is now CASH-ONLY to match: the Pay Online venue button + wallet picker steps were removed (a single static "Pay at the Shop — How you'll pay" card), the summary reads "Paying via cash at the shop", and `applyPlan` always calls `applyDownPaymentPlan(..., { venue: "shop", amountChoice, wallet: "" })` + `materializeCashPendingOrder` then navigates to payment verification with paymentMethod "Cash". Since online down-tier orders now carry `fullPaymentRequired` (not down), staff/admin Payment Verification and the customer page treat them as normal online full payments. Cash down-tier keeps the Partial (50%)/Full choice on the Down Payment Method page exactly as before. Typecheck + build pass.
- MY ORDERS STATUS = STAFF/ADMIN + AUTO-SCALE NO LONGER FIGHTS BROWSER ZOOM (to be pushed, 2026-09-11 1:19 PM PHT): two fixes. **(1) Customer status parity**: the customer My Orders list + status badges in `CustomerOrders.tsx` now display the exact staff/admin status set (Awaiting Payment / In Queue / Printing / Completed / Released / Canceled) — the customer-only "Awaiting Verification" special label was removed from the My Orders badges (a payment with a submitted reference now just reads "Awaiting Payment" there, identical to the staff view); Order Tracking and the customer Dashboard keep their friendlier "Awaiting Verification" / "Ready for Pickup" / "Picked Up" wording. The My Orders filter dropdown already used the shared status set, so only the badge text changed. **(2) Auto-scale no longer fights the browser's own zoom** (fixes the reported zoom-out-then-zoom-in blur): `readRealViewportWidth()` in `screenScale.ts` now measures the SCREEN's CSS-pixel width (`screen.availWidth`/`screen.width` — reflects OS display scaling, NEVER changes with browser page zoom) instead of `visualViewport.width`/`innerWidth` (which change on Ctrl+/-/pinch). Previously the user's browser zoom re-triggered our CSS zoom recompensation, so zooming out made the page zoom itself in/out (oscillation) and every step re-rasterized text at a changing fractional scale → permanent blur. Now our CSS zoom stays fixed at the OS-scaling factor and the user's browser zoom just works on top, crisply. The 1024px gate, 0.6–1.25 clamp, and `--docufy-vh`/`--docufy-vw` remap are unchanged; the vh/vw vars still track the live `innerHeight` so full-height shells keep filling the screen at any browser zoom. Typecheck + build pass.
- AUTO-SCALES TO ANY SCREEN/OS ZOOM + LIVE NOTIFICATIONS (PUSHED — commit 8939df70, 2026-09-11 12:45 PM PHT): two system-wide runtime fixes in `src/main.tsx` + three stores/utils. **(1) Auto-scale**: new `src/app/utils/screenScale.ts` applies a CSS `zoom` on the `<html>` element equal to `realViewportWidth / 1366` (clamped 0.6–1.25, active only ≥1024px) so the compact 1366px-tuned layout renders in the correct proportions on machines with different display scaling/zoom (e.g. a PC at 125% Windows zoom). Root-level zoom behaves like browser page zoom, so Tailwind breakpoints + viewport units re-evaluate against the virtual 1366px viewport and fixed portals stay synced. Real width is read from `window.screen` (`availWidth`/`width` — see the zoom-fight fix above for why it no longer reads `visualViewport.width`), with `visualViewport`/`innerWidth` as fallbacks; `initScreenScale()` wires rAF-debounced `resize` + visualViewport listeners (wired in `main.tsx` before render). Below 1024px scaling is skipped so mobile is untouched. **WHITE-BOX FIX**: a CSS `zoom` on `<html>` does NOT rescale `100vh`/`100vw` in Chromium (they stay at the un-zoomed window size), so `min-h-screen`/`h-screen` shells painted visually short and the white `body` bg showed at the bottom until a manual zoom in/out forced a relayout. `screenScale.ts` now publishes the true layout-space viewport as `--docufy-vh`/`--docufy-vw` custom props (innerHeight/Width ÷ zoom; removed when zoom=1) and `src/styles/index.css` adds UN-layered rules remapping `.min-h-screen`, `.min-h-[100dvh]`, `.h-screen`, `.h-svh` to those vars (the Layout notification dropdown's `calc(100vh/100vw-…)` caps use the vars too) — verified via headless-Chromium measurement (`100vh`=innerHeight vs `100%`=innerHeight/zoom), a DOM-offset replica, and pixel checks on the real build at 1024px (zoom-out) and 1536px (zoom-in). **(2) Live notifications (cross-tab)**: `notificationStore` + `announcementsStore` gained `storage` event listeners on their localStorage keys that reload from storage and notify all in-memory subscribers, so new notifications/announcements appear instantly on the Notifications page + header bell without refresh, INCLUDING from another tab. Both stores refactored to a read/reload/save split (guarded `load`/`loadFromStorage`, unconditional `reload`, `saveToStorage`). Same-tab already worked; this closes the cross-tab gap. Typecheck + build pass.
- FILTER/SEARCH UI UNIFIED SYSTEM-WIDE + SHOP STATUS + NOTIFICATION DROPDOWN (working-tree only, not yet pushed — 2026-09-10): three UI-consistency fixes. **(1) Filter toolbar standardization**: every data-table page now uses the SAME contained filter pattern so the UI reads consistently — on Orders, Payment Verification, and Staff the filter controls (Search + From/To date range + Status dropdown + Reset) were wrapped in a white `Card p-4` toolbar (matching the existing Staff/AdminAttendance contained-Card filter pattern) instead of loose floating controls. The **search input** is standardized everywhere to the `bg-[#FBFDFF]` tint + `ring-1 ring-blue-300` + `pl-10` Search-icon style (was 4 different styles: plain/no-icon in CustomerOrders, `bg-white h-9` in UnifiedPaymentVerification, `pl-9` in Inventory). The **From/To date filters were ADDED to Orders (`UnifiedOrders.tsx`) and Payment Verification (`UnifiedPaymentVerification.tsx`)** — both pages previously had NO date filtering — as Calendar-icon date inputs with cross-clamping (From ≤ To), a "to" separator, and a blue-outline **Clear** button that appears only when a date is set (matching AdminAttendance which already had from/to); date filtering is wired into `filteredOrders` (on `submittedAt`) and `filteredPayments` (on `submittedAt`). Payment Verification's Status/Type/Method dropdowns were restyled to the same `bg-[#FBFDFF] ring-1 ring-blue-300` look as the date inputs so the whole toolbar reads as one unit (was white `bg-white` dropdowns mixed with tinted date inputs). CustomerOrders search got the same icon+tint. Inventory toolbar `pl-9` fixed to `pl-10`. Kept as-is deliberately: the Dashboard/Inventory-Reports preset-range dropdowns (This Month/Last Month/Custom) which are the analytics "period picker" pattern, and status pills/cards/tabs. **(2) Notification dropdown**: the "Mark all as read" button moved from the dropdown footer into the header row where the "{N} unread" text used to be (small blue text link, `text-xs`, only shown when there are unread items — the unread count text itself was removed); the footer now has ONLY the solid-blue "Show All Notifications" button; removed the unused `CheckCheck` import. **(3) Shop Status clarity**: the Shop Status card's description text was status-blind — it always showed "Docufy is currently accepting orders and processing the queue." even when Paused/Closed (when no reason was stored). It is now status-aware: Open shows the accepting-orders line; Paused shows the reason+ETA or "Docufy is temporarily paused. New orders are on hold."; Scheduled Close shows the reason+ETA or "Docufy is closed as scheduled. New orders are on hold.". Typecheck + build pass.
- SERVICES TAB REDESIGN (working-tree only, not yet pushed): rebuilt the Dashboard's EXISTING **Services tab** in `AdminDashboard.tsx` (`ServicesTab`, rendered at `activeTab === "Services"`) into a compact, professional SaaS services workspace — NO new page/route was created; the earlier standalone `/admin/services` prototype was fully reverted (deleted `AdminServices.tsx` and removed its `App.tsx` route, nav item in `navigationConfig.tsx` `adminSections`, and `adminMenuItems.tsx` entry, with their `Layers` imports). `ServicesTab` is now self-contained: it no longer takes the range-scoped `metrics` prop — it accepts `navigate`, holds its OWN local period state, and subscribes to `dataStore` (`dataStore.getOrders()`), with a 5-choice period dropdown (`SERVICES_RANGES`: This Month / Last Month / This Quarter / This Year / All Time; Calendar icon + selected period + chevron, white rounded control on the right of the header) that independently drives its analytics through the existing `getDateRange`/`ordersInRange`/`deriveService` helpers (canceled orders excluded, same derive rules as Overview). **Page header**: Layers chip + "Services" title + "Manage your services and track their performance." subtitle. **4 SummaryCards** (shared component): Total Services (count of services with orders in range), Most Used Service (top service + "% of total orders"), Top Revenue Service (top service + revenue), Inactive Services (0, "Currently unavailable" — no inactive-service concept in the real app). **Two-column row** (`lg:grid-cols-3`): **Service Performance** (left `lg:col-span-2` ≈2/3) — per-service table with Service icon+name / Orders / Revenue / Usage (thin `w-[70px]` `#2F6FD6` progress bar + pct) / Active status badge / ⋮ action → `/admin/pricing`, empty state ("No service activity in this period"), footer "Showing N services" + "View detailed report →"; **Revenue by Service** (right ≈1/3) — compact recharts donut (Docufy blues `#1D73EC`→`#D6E7FB`, `Pie`/`PieChart`/`Cell`) with `fmt(total)` + "Total Revenue" in the center, share legend (color swatch + name + ₱amount + %), "View detailed report →", empty state. **Manage Services** (full-width) — read-only catalog built from the REAL pricing store via new helper `buildServiceCatalog()` (`pricingStore.getMatrix()`): Colored Printing (₱color full a4 / page), Black & White Printing (₱bw text a4), Vellum (₱full a4 / page), Sticker (per sheet), Photo (4R / photo) — plus description, Active badge, and **Edit** + ⋮ per-row → `/admin/pricing`; the **+ Add Service** button (top-right of the card header) also routes there (no new service CRUD/persistence invented). New local helpers `serviceIcon` (Palette/FileText/StickyNote/Image/ShoppingBag/Copy/Printer by name) + `StatusBadge` (emerald dot pill); recharts imports dropped the now-unused `BarChart`/`Bar` and added `PieChart`/`Pie`/`Cell`; lucide added Layers/Palette/Copy/Printer/ShoppingBag/MoreHorizontal/Plus/Pencil/CircleOff/StickyNote/Image. The Overview and Sales tabs and the shared `metrics` prop are untouched. Typecheck + build pass.
- SHOP STATUS / OUTAGE TOGGLE — PHASE A (MANUAL) (working-tree only, not yet pushed — 2026-09-09): staff/admin can now mark Docufy as **Paused** (with a required reason + optional ETA), **Open**, or **Closed (Scheduled)** from a new "Shop Status" card on the Dashboard (Admin Overview gets it directly; the Staff Dashboard reuses the same `AdminDashboard` component so staff see it too). New store `src/app/utils/shopStatusStore.ts` (localStorage `docufy_shop_status_v1` + version key; `ShopStatus = 'open' | 'paused' | 'closed-scheduled'`; `isOperational()` true only when `open`; persists reason/ETA/updatedBy/updatedAt; notifies on transition). **Customer impact**: a shared `ShopStatusBanner.tsx` renders below the header on the Landing Page, Customer Dashboard, My Orders, and Order Tracking (amber when paused — reason + ETA — gray on scheduled close); the checkout page (`PrintTransaction.tsx`) disables both the customer Place Order/Go to Payment Verification AND the walk-in Proceed to In Queue buttons while paused (with an inline amber notice) so no new orders can be placed. **Queue impact** (`UnifiedOrders.tsx`): an amber "Docufy is currently paused" banner sits at the top of the Orders page; starting a new print ("Start Printing" or the clickable Start Here tag) is blocked while paused unless staff explicitly overrides via a "Start Printing While Paused?" confirmation — finishing/releasing jobs already in the pipeline stays available. **Deadline freeze** (`paymentExpiry.ts`): `expireOverdueOrders()` early-returns while paused, so no awaiting-payment order is auto-canceled as expired during an outage. **Notifications**: pausing notifies staff/admin ("Shop Paused") and, individually, every customer with an In Queue / Printing / Awaiting Payment order ("Docufy is Temporarily Paused" — order is safe and on hold, deep-route to /customer/orders); reopening sends "Docufy is Open Again". Supabase heartbeat/auto-outage-detection (Phase B) deliberately OMITTED per user ("for now only the toggle functions. later on the supabase." / "omit the automated ping/warning muna, only manual toggle"). Typecheck + build pass.
- WALK-IN PHOTOCOPY STEPS = 1 & 2 (working-tree only, not yet pushed — 2026-09-08): the walk-in Photocopy flow now uses **Step 1 = Photocopy Options → Step 2 = Review & Complete** instead of the old step-1 → step-4 jump — the step indicator reads 1·2, Step 2 renders the photocopy review (details + manual staff price + Total to Collect + Proceed to In Queue), and the generic "Next Step" bar advances to step 2 (`nextStep = currentStep + 1`; the old `isPhotocopy ? 4 : ...` jump removed). Regular Walk-in Printing and the customer flow are UNCHANGED (still Steps 1–4). In `PrintTransaction.tsx`: photocopy step-4 title → step-2, Step-2 content gated `!isPhotocopy`, the walk-in review renders for `currentStep === 4 || (isPhotocopy && currentStep === 2)` (heading "Step 2: Review & Complete Transaction"), and the walk-in Proceed nav uses the same condition; Back returns photocopy to step 1. Steps/navigation only — data/logic unchanged. Typecheck + build pass.
- ATTENDANCE = SINGLE DAILY SHIFT + "EXCEEDED" EXTRA CLOCK-INS (working-tree only, not yet pushed — 2026-09-08): the time-clock model was simplified from the Morning/Afternoon split to **one Time In and one Time Out per day** covering both AM and PM. `attendanceStore` records now carry a single `timeIn`/`timeOut` pair plus an optional `exceeded` flag and `extraSessions[]`; legacy v1 Morning/Afternoon records migrate to the single pair on load (`upsertSession` → new `upsertTime(userId, name, role, date, field, value)`; `getCurrentPeriod` removed; `sessionBreakMs` now returns 0). **Extra clock-in flow**: staff can still clock in after the day is complete — the staff Timesheet and the Time-In lockout gate first show a confirmation prompt ("…are already done for today. Clock in again anyway? This will be recorded as exceeded time for today…"), and on confirm the store sets `exceeded = true` and opens a new extra session — shown as an amber **"Exceeded"** badge in the staff Personal Time Logs, the timesheet status ("Exceeded for the Day"), and the admin Attendance & Staff Monitoring status column; totals include the extra session (feeds Total Hours/Overtime and the Overtime tab). **StaffTimesheet**: clock widget reworked (Time In → Time Out → Time In Again (Extra)); the Break Time metric card and Morning/Afternoon/Break history columns dropped (table now Date | Clock-In | Clock-Out | Total | Overtime | Status). **StaffTimeInGate**: lockout uses `nextAction !== "time-out"` (no period literals); the disabled "Attendance Complete" button became an active amber **"Time In Again (Extra)"** with a confirm prompt. **AdminAttendance**: single clock-in/out derivation, live-on-clock via `hasActiveSession`, Adjust dialog's Session selector removed (single Time field), reset-day uses `upsertTime`, amber Exceeded badge added, footer note updated. **staffRoster** seeds one continuous shift per staff (Robert 8:00–16:00 On Time, Katie 9:42–17:00 Late, Miguel 8:05–18:05 Overtime). Typecheck + build pass.
- LANDING PAGE HEADER SHOWS PROFILE WHEN LOGGED IN (working-tree only, not yet pushed — 2026-09-08): on the public Landing Page, the header now swaps its "Log In" button for a compact profile chip when a user is signed in — a circular avatar (the profile photo or blue initials), the user's name, and a "customer/staff/admin Account" caption — clicking it goes straight to `/{role}/profile`. Logged-out visitors still see the Log In button (unchanged). Wired via `useAuth` in `LandingPage.tsx`.
- OVERALL UI TEXT VISIBILITY IMPROVED (working-tree only, not yet pushed): system-wide readability pass on the admin/staff UI — secondary and supporting text is darker and more legible at normal desktop viewing with no layout/design changes. **Global design token**: `--muted-foreground` darkened `#717182 → #5C5D6E` in `theme.css`, which immediately raises contrast on every `text-muted-foreground` consumer (CardDescription, DialogDescription, FormDescription/helper text, input/select/textarea placeholders, TableCaption, inactive Tabs). **One-step utility bump** (`text-slate-400 → text-slate-500`, `text-gray-400 → text-gray-500`) applied across the admin-facing surface (24 files: `admin/*`, `shared/*`, `staff/*`, `ui/*`, `Layout.tsx`, `LoginPage.tsx`, `SignUpPage.tsx`, `ForgotPassword.tsx`) — so washed-out subtitles, table secondary info, timestamps, order IDs, card descriptions, empty-state messages, filter/dropdown text, labels, and action-icon grays all read ~one step darker while keeping the slate/gray hierarchy intact (900 → 700/600 → 500 → 300 decorative icons). **Shared components**: `SummaryCard` label default bumped `text-slate-500 → text-slate-600` and subtitle row gained `font-medium` (`text-[11px] text-slate-500`, was slate-400), highlight-variant label `text-white/80 → text-white/90`; `ui/table.tsx` `TableHead` `font-medium → font-semibold` for stronger column headers. **Sidebar navigation** (desktop `Layout.tsx` + mobile `MobileNavSheet.tsx`, all roles): inactive nav items `text-blue-100` (contrast ~3.7:1) → `text-white/90` and label font `font-medium → font-semibold` for parent and child items, so nav links are clearly readable against the Docufy blue while the white active pill stays the strongest element. Deliberately untouched: decorative empty-state icons (slate/gray-300), disabled-state text/branches, primary headings, main values, and all customer-facing + public pages (`customer/*`, `LandingPage`, `JobApplyForm`, `PublicJobApplication`) which still use their lighter `gray-400` text (can be swept later if desired). Typecheck + build pass.
- PAYMENT VERIFICATION PAGE REDESIGN (working-tree only, not yet pushed): rebuilt `UnifiedPaymentVerification.tsx` (admin + staff) into a compact, professional verification workspace per the reference spec — no logic changed, only presentation. Container tightened to `max-w-[1600px]` (was 1800) and `font-poppins` wrapper removed. **Page header**: title + "Review and verify customer payments" subtitle + search field (h-9) upper-right. **Summary cards** (5, `SummaryCard`): Pending Verification (blue Clock, doubles as a Pending/All toggle), Verified Today (green check, counts only payments verified on the PHT `todayPHTKey()`), In Queue (live `dataStore` count of `'In Queue'` orders), Cancelled / Expired (red, live canceled-order count), Total Verified Today (blue `highlight` card, ₱ sum verified today). **Compact filter bar** (replaces the earlier large segmented tab row — no To Verify/All Payments/etc. tabs, and NO sort control): three small `h-9` dropdowns — Status (Pending default so the list always leads with what needs attention / All / Verified / Rejected / Cancelled / Expired), Type (All / Cash on Pickup / Full Payment / Down Payment), and Method (All + every distinct method present in the list) — wired through `statusFilter`/`typeFilter`/`methodFilter`. Row ordering is ALWAYS automatic priority FIFO (pending first, oldest-submitted first with "Next to Verify" on top; then verified then rejected, newest first) — priority is never a manual sort choice. **Table** (one rounded card, 7 columns): PRIORITY (compact `w-7 h-7` circle badge with the verification sequence number inside — filled blue + halo for the first pending row — marking order for pending rows, "Next to Verify" tag on the first; small green ✓ / red ✗ circle chips for verified/rejected), Customer (compact avatar + name + order ID + submitted date·time), Payment Type (pill: Cash on Pickup / Full Payment / Down Payment), Payment Method (icon + name), Amount (kind-aware: cash = total + "Amount to pay at shop" + amber Due `formatPHDateTime(deadline)`; down = total / Paid / amber Balance; full = total + "Paid ₱X · Fully paid"), Payment Status (compact pill: Pending amber / Verified green / Expired / Cancelled / Rejected red — derived from `canceled`/`expired` flags on `cancellationReason`), Action (**View** outline button for EVERY row — NO in-row Verify; clicking the row or View opens the details dialog where staff review before acting). **Details dialog**: customer header (name + submitted date·time), Verification Status badge, Order ID / Payment Type / Payment Method cards, kind-aware amount cards (cash = Amount to Pay + Payment Deadline with live countdown + "Awaiting Payment at Shop — confirm only once paid in cash"; online = Order Total/Amount Paid/Remaining Balance + down/full-required notes), online QR + `PaymentMethodQRPanel`, Reference Number with "View Proof" button (image dialog), then the real actions — **Verify Payment** (green) and **Reject Payment** (red, reason required) — staff only verify after reviewing the details. Priority numbers remain sequence-of-pending by submission time (`pendingSequenceById`); verify/reject flows, auto-queue-on-verify, dialogs, and workflow untouched — no manual add-to-queue, cash deadline (`cashPickupPaymentHours`) and online verification window (`onlinePaymentVerificationHours`) remain admin-configurable Pricing Store Order Rules (0 = no auto-expiry). Data: seeds for the 3 active queue orders (ORD-0002/3/4) moved from 2026-09-02 to 2026-09-06 (today) so the "Verified Today" cards read non-zero (₱310); queue FIFO ordering/statuses unchanged. Typecheck + build pass.
- SALES TAB REDESIGN (working-tree only, not yet pushed): rebuilt `AdminDashboard.tsx` **Sales Tab** into a clean, professional analytics workspace. **Page header**: "Sales" title + "Sales performance and revenue overview." subtitle (the existing date-range selector in the dashboard header controls all data). **KPI row** (4 compact `SummaryCard`s): Total Revenue (DollarSign, formatted ₱ with trend %), Total Orders (Package, count + trend), Average Order Value (ShoppingCart, ₱X.XX + trend), Sales Growth (TrendingUp, signed % in green/red). All use the existing `TrendText` helper and range-scoped `metrics`. **Sales Trend** card (primary, full-width `SectionCard`): "Revenue performance over time" subtitle; segmented Daily / Weekly / Monthly switcher rendered via new `headerRight` prop on `SectionCard` (a subtle `bg-slate-100` pill with active `bg-white shadow-sm text-[#2F6FD6]`), replaces the old loose button row; chart uses `h-52 lg:h-[300px]`, improved tick fill (`#64748b`), `CartesianGrid` stroke bumped to `#e2e8f0` for readability, same `#2F6FD6` area + dot style; `ResponsiveContainer` height is explicit for consistent sizing. **Sales by Service** card (`SectionCard`, left half): compact list of active services (revenue > 0) each with name (truncate), revenue, percentage badge, and thin `bg-[#2F6FD6]` progress bar; "View Services" action switches to the Services tab via `onViewServices` callback; empty state shows `BarChart3` message. **Recent Sales** card (`SectionCard`, right half): responsive 6-column compact table (Order / Customer / Service / Payment / Date / Amount) with `hidden sm/md/lg:hidden xl:table-cell` breakpoints for mobile; shows top 5 non-canceled `recentOrders`; `deriveService` maps service, `formatRelativeTime` dates; "View All" navigates to `/admin/orders`; empty state via `Clock`. Removed: old Highest/Lowest Sales and Sales Periods cards, old loose switcher buttons. `SalesTab` now accepts `navigate` and `onViewServices` props. `SectionCard` gained optional `headerRight?: React.ReactNode` (renders instead of `actionLabel` when present, non-breaking for other usages) and its subtitle text bumped from `slate-400` to `slate-500` for legibility across all `SectionCard` usages. Typecheck + build pass.
- STAFF DASHBOARD = ADMIN OVERVIEW (working-tree only, not yet pushed): the Staff Dashboard is now pixel-identical to the Admin Dashboard's Overview with the two sales cards removed. `AdminDashboard.tsx` is now role-aware via optional props `menuItems`, `role: "admin" | "staff"`, and `userName` (defaults keep admin behavior unchanged). For staff it renders the SAME Overview layout — header with date-range selector (This Month/Last Month/This Quarter/This Year/All Time/Custom), the 4 KPI SummaryCards (Total Sales / Total Orders / Walk-in Transactions / Active Customers with vs-previous-period trends), Recent Transactions (⅓, shows 4, "View All" now routes to `/staff/queue`), and Inventory Snapshot (⅔, "View Inventory" routes to `/staff/inventory`) — but NO Sales Trend card, NO Sales Comparison card, and NO Sales/Services tabs (staff get a single page, no tab bar). `OverviewTab` took a `role` prop (default "admin") and special-cases the two sales cards via `showSales`; `InventorySnapshot` gained an `inventoryPath` prop (default `/admin/inventory`). The greeting uses the staff user's first name (`userName`), and `StaffDashboard.tsx` was rewritten as a thin wrapper passing the staff `menuItems` + `role="staff"` + first name to the same `AdminDashboard` component. Data source stays `dataStore` (same orders admin sees). Staff dashboard remains ungated (no `StaffTimeInGate`), matching the previous behavior. Typecheck + build pass.
- PRIORITY COLUMN = "#" + DUPLICATE-NUMBER BUGFIX (working-tree only, not yet pushed): the Payment Verification and Orders table headers now read **"#"** instead of "Priority". Fixed the "both rows show 2" bug in `UnifiedPaymentVerification.tsx` — the payment record id was `` `PAY-${order.id.split('-')[1]}` `` so every seed order (all `ORD-2026-*`) collapsed to the same `PAY-2026` id, which made the FIFO `pendingSequenceById` map hold ONE entry and both pending rows (ORD-0001 ₱75, ORD-0006 ₱120) render the same last number ("2") — it also made "Next to Verify" match every pending row. Each payment now uses the unique order id (`id: order.id`), so pending rows number 1, 2, 3… in submission order and only the earliest pending gets the "Next to Verify" tag. `id` is only used for React keys/search/FIFO lookups (not displayed), so nothing else changed. Typecheck + build pass.
- SALES TREND MOCK HISTORY (working-tree only, not yet pushed): added 12 historical mock orders (ORD-2026-0015 → 0026, Oct 2025 → Sep 2026) to the `dataStore.ts` `initialOrders` seeds so the Admin Dashboard Overview **Sales Trend** area chart actually renders a multi-month line instead of a single September point (the existing live seeds were all dated 2026-09-01→06). One `Released`/`Completed` order per month with varying totals (₱890 → ₱2,760, incl. a December holiday peak) and varied customers/methods (GCash/Maya/Cash online + walk-in). All are `paymentVerified: true` (so they never show as Pending in Payment Verification, never join the queue, and only feed the sales charts/KPIs + order history); 2026-09 still shows the dip to the live ~₱310 because the month just started. `dataStore` is in-memory (re-seeded on every fresh page load — no storage-version bump needed). Typecheck + build pass.
- QUEUE MOCK SEEDS RESTAGED FOR FIFO (working-tree only, not yet pushed): the `dataStore.ts` seed order statuses were inconsistent with the queue's FIFO processing — the LATEST-submitted queue order (ORD-2026-0004, submitted 10:00 AM) was marked `Printing` while the EARLIER ones (8:30, 9:00 AM) still sat `In Queue`, so its Priority badge (#3) showed the newest job already on the printer ahead of #1/#2. Re-staged so the printer is on the EARLIEST job: ORD-2026-0002 (8:30 AM) is now `Printing` (Priority 1), while ORD-2026-0003 (9:00 AM, Priority 2) and ORD-2026-0004 (10:00 AM, Priority 3) are `In Queue` — so the "Start Here" tag lands on the 9:00 order (the next one to print) and the Time column reads oldest-first. Also normalized ORD-0004's `statusUpdatedAt`/`lastUpdatedAt` from 10:30 to 10:00 (matches when it actually queued, so the displayed time no longer suggests it was placed at 10:30) and updated the notes on both orders. Everything else (logic, priority computation, sorting) unchanged — build passes.
- ORDERS HEADER CLEANED (working-tree only, not yet pushed): removed the "Monitor and manage all print jobs" subtitle, the live clock/date row (clock icon + `03:15:36 PM` + date including the stray separator glyph), and the now-dead `now`/`setInterval` state + `nowPHT`/`subscribeInternetTime` imports from `UnifiedOrders.tsx`. The search/filter toolbar stays; `Clock` import kept (still used by the In-Queue summary card icon). Build passes.
- SIDEBAR POLISH — BRANDED HEADER + FLAT NAV (working-tree only, not yet pushed): the sidebar header (desktop `Layout.tsx` + mobile drawer `MobileNavSheet.tsx`, all three roles) now shows the Docufy circular logo plus a bold white "Docufy" wordmark on the LEFT, with a ChatGPT-style PANEL-LEFT square collapse toggle pinned to the RIGHT of the same row (small `PanelLeft` icon button, no text; desktop toggles collapse/expand, mobile closes the drawer). When the desktop sidebar is collapsed (icon-only) only the centered logo is shown and clicking it expands the sidebar back (with a "Expand navigation" hover tooltip). The mobile header keeps its `Menu` hamburger to open the drawer. The MAIN / OPERATIONS / MANAGEMENT gray uppercase section headings were REMOVED from the admin/staff sidebars: the navs now render as ONE flat list (Dashboard, Orders, Payment Verification, Walk-in, Inventory, + the expandable parent rows Operations → [Attendance, Job Board] and Management → [Staff, Payment Methods, Pricing, Content]). Only the three non-clickable group labels AND the gray parent label shown above each expanded submenu (e.g. the "OPERATIONS"/"MANAGEMENT" caption under the expandable rows) were dropped — the expandable parents, inline children, active-state, and parent-expansion persistence are unchanged. Implemented by flattening the section config (`navigationConfig.tsx` `adminSections`/`staffSections` via `flattenSections`) in both `Layout.tsx` and the `MobileNavSheet` (customers were already flat), and removing the now-unused `NavSection` imports. Typecheck + build pass.
- ORDER LIFECYCLE OVERHAUL — UNIVERSAL PAYMENT GATE + DEADLINES/AUTO-EXPIRY (working-tree only, pending client decisions): every customer order — cash AND online — now starts in **Awaiting Payment** and stays OUT of the queue until payment is confirmed, so nothing is printed before payment. The old `Received` and `On Hold` statuses are REMOVED everywhere; the status set is now **Awaiting Payment → In Queue → Printing → Completed → Released (+ Canceled)**. The whole flow: **customer checkout** (`PrintTransaction.tsx`) — customer orders are always created `Awaiting Payment` with a `paymentDeadline` now computed from two admin-configurable hours in the pricing store's `Order Rules` (`cashPickupPaymentHours` default 48, `onlinePaymentVerificationHours` default 24; `0` = no auto-expiry, both flagged as PLACEHOLDERS pending client confirmation) and a cash `holdReason` is always set; online orders have no holdReason; walk-in orders go straight to `inQueue` (paid at the shop, `paymentVerified: true`) with no deadline. **Auto-expiry** (new `src/app/utils/paymentExpiry.ts`, wired in App.tsx, 30s interval): any `Awaiting Payment` order whose `paymentDeadline` passed AND nothing is verified is auto-canceled → status `Canceled`, `cancellationReason: "Payment Deadline Expired"`, `paymentDeadline` cleared (prevents re-fire), and staff/admin + customer notifications fire; verified orders never expire and orders without a deadline are never touched. Legacy seeds carry far-future `2026-09-30T23:59:00+08:00` deadlines so they stay pending instead of being canceled on load. **Customer Payment Verification** (`customer/PaymentVerification.tsx`): the order is loaded from the store; the old cash "Confirm Order" mutation button was replaced with a guidance/track card — cash means "nothing to submit online; pay at the shop" — and an Amount Paid * input (preset to the required amount) is persisted to the order (`paymentAmountPaid`) when the customer submits/subscribes for online/down payments. **Staff/Admin Payment Verification** (`UnifiedPaymentVerification.tsx`): `PaymentType` gained payment-kind data (`kind: cash | online | online-down`, `totalAmount`, `amountPaid`, `remainingBalance`, `deadline?`); the detail dialog now distinguishes Cash on Pickup (Amount to Pay, Payment Deadline + live countdown, "Awaiting Payment at Shop — confirm only once paid in cash") from Online Full (Order Total / Amount Paid / Remaining ₱0) and Online Down (Total / Paid / Remaining Balance due on pickup); approving a payment now sets the order status to **In Queue** (was `Received`), so the order enters the queue AUTOMATICALLY — staff never manually add it. **Queue page** (`UnifiedOrders.tsx`): `OrderType`/status form/cards all trimmed to the new set; summary cards are All Orders (queue), In Queue, Printing, Completed, Released, Canceled (Received/On Hold cards removed); the "Start Here" next-to-process tag now targets the earliest **in-queue** order and detail actions are CONTEXTUAL — In Queue → Start Printing (or Cancel Order), Printing → Mark as Completed, Completed → Release Order, Awaiting Payment → auto-queue note + Cancel, Released/Canceled → no actions; cancelling now persists `cancellationReason` (shown in the detail dialog alongside the auto-expiry reason) and requires the reason in the form; a manual guard blocks moving an unverified awaiting-payment order into the queue. **Dashboards** (StaffDashboard): On Hold KPI → Awaiting Payment, received/onHold filters, badge maps, and message lists all updated; `invoiceUtils` fallback default now `Completed`. `dataStore`/`ordersStore` round-trip the new fields (`paymentDeadline`, `paymentAmountPaid`, `cancellationReason`, plus `paymentReferenceNumber`/`paymentProofUrl`/`paymentVerified`), legacy `Received`/`On Hold` entries migrate to `inQueue` on load, and `getOrderStats` dropped received/onHold (new keys: total/awaitingPayment/inQueue/printing/completed/released/canceled/inProgress). `orderStatusPalette` received/onHold styles removed. Typecheck + build pass.
- DOWN-PAYMENT TIER (₱50–99): CUSTOMER CHOOSES HOW & HOW MUCH TO PAY (working-tree only, pending client decisions). For orders totaling ₱50–99 (`DOWN_PAYMENT_THRESHOLD`), the Step-4 "How would you like to pay?" panel now offers TWO choices instead of a fixed 50% down-payment rule: **(1) Pay at** — `Pay at the Shop` (Banknote icon: customer visits the shop and pays cash; order stays awaiting down payment and is NOT processed until verified at the shop) or `Pay Online` (Smartphone icon: pays with an online method, order stays awaiting payment verification after the reference is submitted); **(2) Amount to pay today** — `Down Payment (50%)` (pay half `₱total×0.5` now, remaining balance on pickup) or `Full Amount` (pay everything now, nothing left on pickup). New `PrintTransaction.tsx` state `paymentVenue: "shop" | "online"` (default online) + `paymentAmountChoice: "down" | "full"` (default down, both reset in `resetForm`); derived flags `isDownTier`/`isFullTier`/`requiresDownPayment = isDownTier && choice==="down"`/`requiresFullPayment = isFullTier || (isDownTier && choice==="full")`/`downPaymentValue = total×0.5`; `cashDisabled = isFullTier || (isDownTier && venue==="online")` now drives the Cash on Pickup card (replaces the old `cashRestricted`) — venue effects auto-set `paymentMethod="cash"` for Pay-at-the-Shop and clear cash for Pay-Online, and the online method cards go dashed/disabled (`onlineDisabled = isDownTier && venue==="shop"` with grayed icon/label + hidden View QR) while the cash card shows a venue-aware "Not available — pay online instead" message. `handleSubmit` derives `fullPaymentRequired`/`downPaymentRequired`/`downPaymentAmount` from `flowTier`+choice (NOT just the total), and the online holdReason now states the actual amount ("down payment of ₱X (50% of total ₱Y) is pending verification" vs "full payment of ₱X is pending verification"); cash holdReasons say "pay this amount at the shop before your order can be printed/processed". The incl. cash guard is now `cashDisabled`-based with tier-aware toasts. Dynamic venue info note (blue "You selected Pay Online — choose your payment method below..." vs amber "You selected Pay at the Shop — pay at least ₱X (50%)... not processed until verified...") + live "Due Now (Full Payment)" / "Down Payment Due Now" vs "Balance on Pickup" (`₱0` when full, `₱total×0.5` when down) summaries. Success modal + confirm-dialog description switched from `paymentRequirementFor(calculateTotal())` to the choice-derived `requiresFullPayment`/`requiresDownPayment` flags (title "Order Submitted — Full Payment Required/Down Payment Required", "Full/Down Payment Summary" boxes, "Pay ₱X via {method or Cash at the shop}"). Fix: `UnifiedPaymentVerification.handleVerifyPayment` now only sets `fullPaymentVerified: status==="verified" && !!order.fullPaymentRequired` (looked up via `dataStore.getOrders()`) so down-payment orders no longer carry a stray full-payment-verified flag. Walk-in Step 4 (separate branch, no payment selector) is unaffected. Typecheck + build pass.
- WORKFLOW-CLARITY FEEDBACK (queue priority, verification order, Cash on Pickup ack, policies, full-payment rule): a working-tree-only implementation (NOT pushed — pending client decisions). **QUEUE PROCESSING ORDER** (`UnifiedOrders.tsx`): orders now default-sort by `submittedAt` ASC (true FIFO) instead of last-updated, so the queue reads Oldest First everywhere (details, filters, staff/admin); added a `Priority` column whose numeric badge is the processing sequence (`priorityById` over queueOrders); the first `received`/`inQueue` order dynamically shows a blue "Start Here" tag (only when the status filter is "All Orders"); `nextToProcessId` = earliest-submitted received/in-queue order, so staff always know which order to print next — and the **Start Here tag is now CLICKABLE** (`StartHereTag` gained an optional `onClick`): clicking it (`handleStartFromHere`) opens the status-update form to move that order to **Printing**, so after confirming, the order leaves received/inQueue and the tag advances to the next waiting order (or disappears when none remain). Non-clickable caller (`UnifiedPaymentVerification` "Next to Verify") is unchanged. **PAYMENT VERIFICATION ORDER** (`UnifiedPaymentVerification.tsx`): Pending payments now display FIRST (FIFO), then verified then rejected (most recent first); sequenced (① ② ③) "Priority" badges with a "Next to Verify" tag on the first pending; verified = green Check, rejected = red X; approving now also records `fullPaymentVerified`. **CASH ON PICKUP ACKNOWLEDGMENT**: new shared `CashOnPickupAcknowledgement` checkbox ("By selecting Cash on Pickup, you agree to claim and pay for your order in full upon pickup. Unclaimed orders may result in restrictions...") required before placing/dispatching any cash (CoP) order — rendered at checkout (`PrintTransaction.tsx` Step 4 next to the payment method card) and on the customer Payment Verification page ("Confirm Order" stays disabled until checked). **TERMS & PRIVACY AT CHECKOUT**: new shared `LegalPolicyDialog.tsx` (Terms/Privacy tabs + "Draft — for review only" banner, content mirrors the Landing/Sign-Up modals, "Last updated: April 27, 2026") reachable via an "By placing your order, you agree to our Terms & Conditions and Privacy Policy." link under the total in the customer checkout. **FULL-PAYMENT RULE (high-value orders)**: added admin-editable `fullPaymentThreshold` to the pricing store (default **₱100**; boundary `>=`; client told me to "try to apply it" after naming both ₱100 and ₱150). Orders at/above the threshold now require **FULL payment** (no 50% option): status → 'Awaiting Payment' + full-amount holdReason, `fullPaymentRequired`/`fullPaymentAmount` persisted through dataStore/ordersStore round-trips and localStorage, amber "Full Payment Required — 100% Upfront" block at Step 4 (Amount Due = total, Down Payment Option = Not Available), full-payment success-modal + notification variants. **Cash on Pickup is NOT selectable for customers on such orders** — the cash card is dashed/disabled (`cashDisabled`), a customer's cash selection is auto-cleared, and a defensive `handleSubmit` guard shows a tier-aware toast — so high-value customer orders are paid online (awaiting payment verification). Walk-in (staff-assisted) high-value orders are unaffected (separate Step-4 branch; cash verified at the shop). Step-4 requirement UI is now three-way (Full / 50% Down / None) plus a payment summary in checkout. **PAYMENT SUMMARY ON ORDER DETAIL** (shared `OrderPaymentSummary.tsx`): the admin/staff order detail dialog now shows Total, Amount Paid, Remaining, and status (Paid / Partially Paid / Payment Due) derived from payment/down-payment/full-payment verification flags. Typecheck + build pass.
- UNIFIED SUMMARY CARD COMPONENT (standardized KPI/stat cards, styling only): new shared `src/app/components/ui/summary-card.tsx` — a single `SummaryCard` built EXACTLY on the Admin Dashboard Overview master design (the old `StatCard`): `w-11 h-11 rounded-xl` blue icon chip + `w-5 h-5` icon on the LEFT, label `text-xs font-medium text-slate-500` truncate, value `text-xl sm:text-2xl font-semibold text-slate-900 leading-tight`, optional `subtitle` row (`text-[11px] text-slate-400`), card shell `p-4 rounded-xl border border-slate-200/70 bg-white shadow-[0_1px_2px_rgba(15,23,42,0.04)] flex items-center gap-4`. Props keep status/color variation legal (`iconBg`/`iconColor`/`labelColor`/`valueColor`, clickable hover border, `active` filter state = accent border + tinted bg, `highlight` filled-blue variant for the Payment Verification "Total Verified" card). The AdminDashboard Overview `StatCard` was DELETED and its 4 KPI cards (Total Sales/Total Orders/Walk-in/Active Customers) now render through `SummaryCard` (trend row preserved via a new local `TrendText` helper). Every standalone summary/KPI/stat card factor was converted to the SAME component so the icon-left / label-above-value / identical size+typography+padding structure is identical everywhere: Staff Dashboard 4 KPI cards, InventoryManagement main Items/Low-Stock cards + Reports-tab 5 stat cards (keep their green/amber/red status value colors), AdminAttendance 4 KPI cards (corner-icon + hover-fill-to-blue layout removed), PaymentMethodsManagement 3 cards, UnifiedPaymentVerification 4 filter cards + highlighted Total Verified (local `SummaryCard` removed), StaffTimesheet 3 metric cards (Today value + "Today · This week: X" subtitle), and UnifiedOrders 8 order-status filter cards (kept pastel meaning via status icon chip/label colors + active border/bg, click-to-filter preserved). Grid widths/columns stay per-module/responsive; only the card structure is unified. NOT touched (deliberately): Dashboard Sales/Services tab icon-less summary cards (already master-consistent), the Dashboard InventorySnapshot mini-tiles, and the Inventory "Papers Left" content card — table cards, form containers, content/detail/modal cards are unchanged. Dashboard Sales/Overview icon imports unchanged. Typecheck + build pass.
- COMPACT RESPONSIVE LAYOUT (styling only, layout preserved): tightened the app-wide spacing so the dashboard and management pages feel consistently dense on small-scale laptops (100% display scaling, 1366×768) without changing card placement, data, functionality, or colors. NOTE: the earlier "Root font stays 16px (no zoom/transform workaround)" intent is SUPERSEDED by the AUTO-SCALE feature below (working-tree only, not yet pushed — see the September 11 pushes when pushed). Shared `Layout.tsx`: page content wrapper padding went `p-3 sm:p-6 lg:p-8` → `p-4 sm:p-5 lg:p-6` (desktop margins 32px→24px, standard 4px rhythm throughout all pages) and the top header horizontal padding aligned to match (`px-4 sm:px-5 lg:px-6`); header height, sidebar width (`w-64` / collapsed `w-[72px]`), and the blue sidebar styling are unchanged. Admin Dashboard: outer container and each tab (`Overview`/`Sales`/`Services`) spacing `space-y-6`→`space-y-5`, section-grid gaps `gap-6`→`gap-4 sm:gap-5`, `SectionCard` header `px-6 pt-5 pb-3`→`px-5 pt-4 pb-2.5` with body `px-6 pb-5`→`px-5 pb-4` and title weight `font-bold`→`font-semibold`, KPI `StatCard` padding `p-4 sm:p-5`→`p-4`, Chart heights trimmed (Sales Trend `lg:h-[280px]`→`[260px]`, Sales tab chart 320→300, Revenue by Service `lg:h-[300px]`→`[280px]`), Recent Transactions rows `py-2.5`→`py-2`, and the tab bar `py-3`→`py-2.5`. ADMIN DASHBOARD OVERVIEW CARD GRID (working-tree only, not yet pushed): the four overview cards keep their row/column ORDER but now follow a deliberate SMALL → LARGE hierarchy — Row 1 = Recent Transactions (narrow **⅓**, left) + Sales Trend (wide **⅔**, right); Row 2 = Sales Comparison (narrow **⅓**, left) + Inventory Snapshot (wide **⅔**, right) — the wide cards give the Sales Trend chart and Inventory Snapshot more horizontal room, and the Recent Transactions list is compressed to the ⅓ column (its body keeps a `lg:min-h-[260px]` fill so the two cards in each row align top and bottom). The Recent Transactions "See all N transactions" expander button and its `showAllRecent` state were removed — the list now shows the 4 most recent orders (`slice(0, 4)`; the card's "View All" header action still navigates to the Orders page). Implemented purely by moving the `lg:col-span-2` utility between the cards inside the two existing `grid-cols-1 lg:grid-cols-3 gap-4 sm:gap-5` containers — data, order, card titles, charts, KPI cards, and logic unchanged; typecheck + build pass. Staff and Customer dashboards mirrored the same vertical rhythm (`space-y-6 pb-10`→`space-y-5 pb-8`). Typecheck + build pass.
- ADMIN DASHBOARD REDESIGN (visual polish, layout preserved, blue sidebar KEPT): restyled the shared `Layout.tsx` sidebar, header, and canvas plus the Admin Dashboard cards/charts/tables as a clean, modern SaaS aesthetic WITHOUT moving any card (Overview stays 4 KPI cards → Sales Trend (2/3) + Sales Comparison → Inventory Snapshot (2/3) + Recent Transactions; Sales/Services tabs unchanged). The sidebar REMAINS Docufy blue (`bg-[#1D73EC]`) with its established identity — white/light text and icons, section labels as blue-100/70 uppercase, white active pills (`bg-white text-[#1D73EC]`), 2px white/40 dividers, white profile avatar with blue initials — only its spacing/alignment/typography were refined. The canvas went `#f0f4f8` → neutral `#f6f7f9`, and the top header dropped its drop shadow (white, slate bottom border). The mobile nav sheet (`MobileNavSheet.tsx`) mirrors the same blue sidebar. Dashboard cards use `bg-white` + `border-slate-200/70` + a barely-there shadow (`0 1px 2px rgba(15,23,42,0.04)`) with `rounded-xl`; KPI StatCards got a unified blue icon chip (`bg-blue-50 text-[#2F6FD6]` — the old green/purple/orange tints are gone), a small muted label, a large semibold value, and a green/red trend arrow with a muted caption; Sales/Services summary cards and the Services breakdown table got the same label/value hierarchy (small slate-500 labels, larger semibold values; table headers now tiny slate uppercase with more row padding + hover tint); Recent Transactions' "Walk-in" chip went purple → slate; all three chart tooltips are now rounded with a soft border/shadow and the area lines are thinner with smaller dots. Header page title uses semibold tracking-tight. NO functionality, data, routes, or card positions changed — this is styling only. Typecheck + build pass.
- STAFF GET FULL INVENTORY ACCESS (SAME AS ADMIN): replaced the staff read-only `StaffInventory.tsx` (previously just a stock-level grid) with a thin wrapper that reuses the shared admin `InventoryManagement.tsx` component (`export default function InventoryManagement({ menuItems = adminMenuItems, title = "Inventory Management" }: InventoryManagementProps)` — new optional props so the SAME full module serves both roles). Staff at `/staff/inventory` now have every capability admins do: Add/Edit items, Archive/Restore, Stock In / Stock Out, Papers Left card, Low-Stock banner, summary cards, Active/Archived + search toolbar, and the Reports tab (material usage, stock-movement bar chart, inventory alerts, Stock-In/Out history, Current Inventory report). Stock actions record the acting user via `user?.name` (same as admin). The inventory "View Inventory" notification action now lands staff on the full management page for both low-stock and out-of-stock alerts. Build passes.
- NOTIFICATION DROPDOWN — LARGER, READABLE PANEL + CLICK = VIEW FIRST: the header bell dropdown is now a proper notification panel instead of a small compact popover. Width grew `20rem→24rem` with a viewport-capped max-height (`max-h-[min(32rem,calc(100vh-6rem))]`) as a flex column so the header + footer buttons stay fixed while only the list scrolls. Each notification item is roomier/more legible (more padding, 40px icon chip with bigger icon, larger title/message/time text, bigger unread dot); the panel header shows the title plus an "N unread" count; the empty state is larger; the "Mark all as read" (now with a CheckCheck icon) and "Show All Notifications" buttons are taller (`h-11`) with comfortable spacing. Responsive on mobile (stays within `min(24rem, calc(100vw-1.5rem))`, right-anchored, height-capped) so there is NO horizontal scrolling. CLICK BEHAVIOR: clicking a notification/announcement in the bell dropdown no longer just silently marks it read and closes — it now opens a DETAIL DIALOG showing the type badge (Order / Payment / Status Update / Inventory Alert, or the announcement type), an Important/Emergency priority badge when applicable, the full title, the COMPLETE message, and the PHT date/time; mark-as-read happens ONLY once the detail is opened/viewed (never before). If the notification has a destination, the dialog shows a primary action button — "View Order" / "View Inventory" / "Go to Page" — that navigates to the relevant page (customer order tracking, staff/admin orders list, inventory for alerts); notifications without a destination show a plain Close. Already-read notifications still open the same detail view; notifications are never deleted/hidden when marked read and stay in the full Notifications page/history; "Mark all as read" remains its own separate action. Implemented in `Layout.tsx` (new `selectedDetail` state + `BellDetail` type, `openNotificationDetail`/`openAnnouncementDetail`, `formatDetailTime` via `toPHT`, a `NOTIF_TYPE_LABEL` map; `handleAnnouncementClick` removed, `handleNotificationClick` refactored to navigation-only used by the dialog's action). Build passes.
- NOTIFICATIONS — SIDEBAR TAB REMOVED, ACCESS VIA HEADER BELL DROPDOWN: removed the dedicated "Notifications" sidebar tab/item from EVERY sidebar (desktop left sidebar, admin menu, staff/customer mobile `MobileNavSheet`, and all per-page `menuItems` arrays passed to Layout) — the buried urgent-announcement dot on that sidebar item and its now-unused `urgentAnnouncements` state in `Layout.tsx`/`MobileNavSheet.tsx` were removed too (no empty space or broken nav item remains). The original Notification page/routes (`/customer/notifications`, `/staff/notifications`, `/admin/notifications`) are UNCHANGED and still exist. The header bell icon stays fully functional on all screen sizes with its unread-count badge and dropdown; a NEW prominent solid-blue "Show All Notifications" button was added at the BOTTOM of that bell dropdown (below "Mark all as read") that closes the dropdown and navigates to the role's original Notifications page (`handleShowAllNotifications` → `navigate('/{role}/notifications')`). So access changed from Sidebar → Notifications → Page to Bell → Preview → Show All Notifications → Page, with no duplicated pages, no bottom nav, and no broken routes. Build passes.
- LANDING PAGE "GET STARTED" CTA — ALWAYS SOLID FILLED (`LandingPage.tsx`): the hero "Get Started" button (the Landing Page's primary CTA, navigates to `/signup`) is now a always-filled primary-color button instead of the old white/outline style. It uses `bg-[#1D73EC] text-white` (Docufy customer primary blue with white text for contrast), a desktop hover that darkens to `hover:bg-[#10316B]` (plus the shared button's slight lift), and an `active:scale-95` pressed/active state for touch devices. Stays `size="lg"` (`h-10 px-6`, comfortable mobile tap target) with a subtle blue shadow for extra prominence. Responsive and consistency applied everywhere — it never switches to an outline/transparent style at smaller widths. Functionality/destination unchanged (`/signup`). Build passes.
- MOBILE HEADER — PROFILE ICON REMOVED, ACCOUNT VIA SIDEBAR ONLY (`Layout.tsx`): the top-right circular avatar/profile button (and its dropdown) is now hidden at mobile widths for ALL roles — `{!isMobile && (...)}` wraps the header avatar + `Profile Settings`/`Home`/`Sign Out` dropdown — so the mobile header stays clean at `☰  title  🔔` (hamburger left, page title, notifications right) with no empty gap. Desktop is unchanged: staff/admin/customer still open their profile from the top-right avatar AND the desktop left sidebar (the sidebar profile button already renders only on `!isMobile` inside the `<aside>`). On mobile, account access is exclusively through the hamburger → `MobileNavSheet` sidebar, which ALREADY has a clearly-tappable account section at its bottom (avatar + email/role + dropdown `Home` / `My Profile` / `Sign Out`); `navigateAndClose('/role/profile')` closes the sheet and lands on the full profile page (Account Settings / Personal Information / Change Password all live there), and the browser Back / profile back-arrow return to the previous page naturally since it uses the normal router history. No separate profile system created; existing account functionality untouched. Build passes.
- ENTER KEY = PRIMARY ACTION (system-wide): pressing Enter in any dialog, confirmation prompt, or form now triggers that context's primary action (submit/confirm) instead of nothing or a stray newline — while never hijacking multiline text areas (Enter still inserts a new line there) and never double-submitting. Centralized in the shared dialog so it applies everywhere with one change.
  - **Shared `ui/dialog.tsx`**: while a dialog is open, a document-level Enter listener (added per-open, removed on close) finds the dialog's primary action — a `button[type="submit"]` first, else an element marked `data-primary-action` — and clicks it exactly once after `preventDefault()` (blocks native double-submit). It skips when focus is inside a `textarea` (preserves newline) or outside that dialog, and never intercepts `Shift+Enter`.
  - **`ui/confirmation-dialog.tsx`** now submits on Enter: header + phrase field + footer wrapped in a `display:contents` form whose submit handler runs the confirm action, and the confirm button is now `type="submit"` (kept `disabled={!canConfirm || loading}` for destructive/cancel/requirePhrase safety); removed the legacy `onKeyPress`/`handleKeyPress` dead logic. Covers all 59 confirmation usages (Place Order, Cancel Order, Stock Out/In, Approve/Reject Payment, Delete Notification, Reset Pricing, Sign Out, Time In/Out, etc.).
  - **Formless admin CRUD dialogs** got `data-primary-action` on their primary button so Enter triggers it (previously did nothing since there was no `<form>`): Inventory Add/Edit + Stock In/Out, Staff Register/Edit, Payment Methods Add/Edit, Pricing Management matrix price + legacy rate editors, AdminAttendance Adjust Time, and AdminProfile Change Password. Form-based dialogs (Job Board, Job/Staff apply, Login/SignUp/ForgotPassword, customer Payment Verification) already submit natively via their `type="submit"` button + `<form onSubmit>`. Read-only detail/invoice dialogs (no meaningful primary action) intentionally left alone. Build passes.
- SYSTEM-WIDE CONFIRMATION DIALOGS (in-app, no browser alerts): rebuilt `src/app/components/ui/confirmation-dialog.tsx` so every significant data-changing action confirms with an EXACT action-name confirm button (`Place Order`, `Cancel Order`, `Stock Out`, `Approve Payment`, `Reject Payment`, `Delete Notification`, `Reset Pricing`, `Sign Out`, `Time In`/`Time Out`, etc.) and a SAFE cancel label (`Go Back`/`Keep Item`/`Keep It`). `requirePhrase` is now OPT-IN (default false — old component always forced typing "Docufy") and reserved for irreversible actions only (Payment Method delete, Pricing reset, Order cancellation, Payment rejection, Attendance reset day, Notification delete). Optional `loading` prop shows a `Loader2` spinner + disables both buttons as a duplicate-submit guard. Destructive styling reserved for deletes/rejects/cancels; positive/save/approve actions use the non-destructive state. Added confirmations to: customer NewPrintRequest Place Order, customer PaymentVerification Submit Reference/Confirm Order, walk-in Proceed to In Queue, JobApplyForm Submit Application, Notifications delete (replaced legacy `confirm()`) + send broadcast, Layout (desktop sidebar + top-right) + MobileNavSheet + all three profile pages Sign Out, StaffTimesheet Time In/Out, and Staff admin Activate/Deactivate (now exact labels). Zero `window.confirm()`/`alert()` calls remain in src. Build passes.
- SESSION PERSISTENCE + LEGACY ORDERS DELETED: **AuthContext persists the logged-in user** across page reloads (localStorage `docufy_auth_session`, lazy-initialized in `useState` via `readStoredUser()` with shape validation, written on every user change, cleared on logout) so refreshing while signed in no longer bounces to `/login`; the 15-min inactivity auto-logout still clears it. Deleted the dead/unrouted legacy order pages `src/app/components/staff/StaffQueueBoard.tsx` and `src/app/components/admin/AdminOrders.tsx` and removed their dead imports in App.tsx (they were previewed first via temporary `/staff/legacy-queue` + `/admin/legacy-orders` routes, then removed). Removed the now-stale `StaffQueueBoard` mention in StaffDashboard.tsx comments. Live order pages remain `StaffOrdersUnified` (`/staff/queue`) and `AdminOrdersUnified` (`/admin/orders`).
- PHOTO AS PER-FILE PRINT TYPE + STEP-1 SERVICE SELECTOR REMOVED (customer `NewPrintRequest.tsx` + shared `UnifiedWalkInTransactions.tsx`, BOTH flows): the Step-1 service-type selector (Document/Vellum/Sticker/Photo radio grid) and its global Photo config panel were REMOVED. Every order now uploads files in Step 1, and Photo is now a per-file **Print Type** in Step 2 alongside Document/Vellum/Sticker. `FileData.printType` widened to include `"photo"` and gained `photoSize`/`photoFinish`/`photoQty` (upload defaults: `'2R'`/`'matte'`/`1`; `PhotoSizeKey`/`PHOTO_SIZE_LABELS` from pricingStore). Both files dropped the standalone global `photoSize/photoFinish/photoQty` state + `calculatePhotoTotal`/global `photoTotal` summary (removed from `calculateTotal`); `serviceType` kept at default `'document'` purely as a fallback for files whose printType is unspecified. `calculateFileTotal` branches: photo → `matrix.photo[size].price * max(1, qty)`; document → `getPriceFromMatrix((printType as ServiceType) || serviceType, ...)`. Photo min-qty enforced per-file on submit (error `"{size} photos ({qty}) require a minimum of {min} pcs"`; for walk-in the same on Proceed). Step 2 Print Type buttons restyled EXACTLY like the Back button — `<Button variant="outline">` in a `grid-cols-2 sm:grid-cols-4` grid, icons Layers/FileText/StickyNote/Camera, active `bg-[#F2F7FF]`, gating the photo panel or the doc content/paper/copies/color/notes grid behind a fragment (`printType === "photo" ? <photoPanel> : <>{docGrid}</>`). Step 1 heading fixed (customer "Step 1: Upload Documents"; walk-in "Step 1: Customer Information & Upload Files") and the old `{serviceType !== "photo" && <>...</>}` upload wrapper removed (both the opening and its closing fragment). Step 4 per-file cards show "Print Type:" + (photo → Photo Size/Finish/Quantity/Price grid; doc → mapped content/paper/copies/color breakdown). Customer "See Colored Pricing Breakdown" dialog now shows photo info (size/finish/qty/price + min note) when the file's printType is photo, and the doc `Paper size` row is capitalized (`bFile.paperSize.charAt(0).toUpperCase()...`). Navigation updated in BOTH flows: Back `setCurrentStep(prev => prev - 1)`, Next `setCurrentStep(prev => prev + 1)` (the 1→4 / 4→1 / photo jump shortcuts removed), Next/Step-3 disabled `(currentStep === 1 && files.length === 0) || analyzingFileId !== null` (walk-in Next) / customer Next-step gate + Step-4 Proceed disabled when `files.length === 0`. Walk-in `handleProceedToQueue` builds a unified order from files (hasPhoto → `type:'Photo'`, photo notes join size/qty/finish, `paperSize`/`copies`/`pages` from the firstPhoto; `attachedFiles` type detection PDF/Document/PowerPoint/Excel/Image). DEAD FILE-PREVIEW UI REMOVED SYSTEM-WIDE: `file-attachments.tsx` dropped its inert "View" button, `onView` prop, and now-unused `Eye`/`Button` imports; `OrderTracking.tsx` removed dead `handleViewFile` + its `onView={handleViewFile}` (kept live "View Invoice"/"View Page Color Breakdown"); `UnifiedOrders.tsx` removed the dead `onView={(file) => {...}}` from its `<FileAttachments>`. Removed now-unused `Eye` (both flows) and `SERVICE_TYPE_LABELS` (walk-in) imports. Photo is now a per-file print type requiring an uploaded file — orders are always file-based. STEP-2 PRINT TYPE HOVER/ACTIVE THEMING: the four Print Type `<Button variant="outline">`s (both flows, customer `NewPrintRequest.tsx` + shared `UnifiedWalkInTransactions.tsx`) add `group` to the button and `group-hover:text-white` to each icon (Layers/FileText/StickyNote/Camera, default `text-[#2F6FD6]`) and the label span (`text-gray-900`) — so when the `outline` variant's `hover:bg-[#2F6FD6]` repaint fills the button dark blue, the blue icon and dark label turn WHITE and stay visible instead of vanishing. SELECTED STATE = FILLED BLUE: the active button switches its class to `bg-[#2F6FD6] text-white` (was the light `bg-[#F2F7FF]` tint), with the icon and label conditionally `text-white` when selected (else the default blue icon / gray label) and still `group-hover:text-white`. CLICK ANIMATION: the buttons add `transition-all duration-150 active:scale-95` so selecting a type gives a subtle press-in scale animation. The click handler still just sets `printType` to a fixed value (no toggle), so re-clicking the same type cannot actually clear selection — the perceived "deselect" was the broken hover theming. Build passes.
- PER-FILE PRINT TYPE GATE (multi-service order entry): in BOTH the customer `NewPrintRequest.tsx` and the shared `UnifiedWalkInTransactions.tsx` Step 2, the per-file "Content Type" select was replaced with a **Print Type** selector (Document / Vellum / Sticker buttons, mirroring the Step 1 service-type selector style). Until a file has a print type chosen, that file's remaining print options (Content Type, Paper Size, Copies|Page Range, Color Mode, Notes, File Total, Preview/Apply) stay HIDDEN behind a dashed "Select a print type above..." placeholder — applied per-file so multi-file uploads each gate independently. `FileData` gained `printType: "" | "document" | "vellum" | "sticker"` (default `""`, applied on upload and copied by "Apply Settings to All Files"). `calculateFileTotal` now prices via `getPriceFromMatrix((fileData.printType as ServiceType) || serviceType, ...)` so each file's total reflects its own print type instead of the global Step 1 service only. PRICING TEXT REFLECTS PRINT TYPE: new `matrixRatesFor(fileData)` helper returns bw/partial/full per-page rates for a file's printType+contentType+paperSize; the customer's "See Colored Pricing Breakdown" dialog now reads from `breakdownFileId` and shows the chosen print type's matrix rates (Document/Vellum/Sticker label + paper size + content type summary) instead of the legacy flat `pricing.bw/colorLow/colorHigh`; the per-file Color Mode B&W legend + Step 4 col >50%/=50%/B&W breakdown + WalkIn's color-mode auto-pricing text all switched from `pricing.*` to `matrixRatesFor(fileData).(bw|partial|full)`. OrderTracking legacy read-only breakdown left as-is (orders don't persist per-file printType). Build passes.
- PRINT OPTIONS SIMPLIFICATION (order-entry forms, all flows): removed the Orientation, Two-Sided Printing, Pages per Sheet, Margins, and Scale controls from the per-file print-options step in BOTH the customer `NewPrintRequest.tsx` and the shared `UnifiedWalkInTransactions.tsx` (walk-in/staff), and moved the **Page Range** selector into the grid cell where Two-Sided previously sat (so Page Range now pairs with Number of Copies). Resulting Step 2 options per file: Content Type, Paper Size, Number of Copies | Page Range (+ specific-pages input), Color Mode, Notes. Also stripped the removed fields from the read-only detail/invoice views: `OrderTracking.tsx` (customer per-file detail grid + invoice) and `UnifiedOrders.tsx` (admin/staff invoice) no longer show Orientation/Two-Sided/Margins/Scale/Pages-per-Sheet. Dead (unrouted) legacy order pages (`AdminOrders.tsx`/`StaffQueueBoard.tsx`) were later DELETED (see the top "SESSION PERSISTENCE + LEGACY ORDERS DELETED" bullet). The `FileData` type still carries the fields (defaults like `pagesPerSheet:'1'`, `twoSided:'no'`) so persisted order data stays compatible and `calculateFileTotal`'s pages-per-sheet divisor still works (defaults to 1 = no divisor). Build passes.
- MULTI-DIMENSIONAL PRICING MATRIX (Docufy pricing, phase 1 â€” store + admin only): `src/app/utils/pricingStore.ts` gained a full pricing matrix on TOP of the existing flat legacy `PricingValues` (existing consumers NewPrintRequest/WalkIn/OrderTracking/LandingPage/ContentManagement/UnifiedOrders still read the legacy fields and keep working â€” the legacy rows are now labelled "Legacy Per-Page Rates" on the admin page). New types: `ColorTier` (`bw`/`partial`/`full`), `ContentType` (`text`/`textWithImage`/`imageOnly`), `PaperSizeKey` (`short`/`a4`/`long`), `PhotoSizeKey` (`2R`..`6R`/`A4photo`), `ServiceType` (`document`/`vellum`/`sticker`/`photo` â€” `photocopy` REMOVED), and `PricingMatrix` (nested: document[content][color][size], vellum[color][size], sticker[color], photo[].price/minQty). `DEFAULT_MATRIX` holds all the published prices (Document Standard, Vellum image-only, Sticker A4 per-sheet, Photo sizes + min qty: 2R â‚±10 min 6, 3R â‚±15 min 4, 4R â‚±20 min 4, 5R â‚±30 min 2, 6R â‚±40, A4 â‚±60). `STORAGE_VERSION` bumped 1.0â†’2.0 (reseeds; matrix persisted in the same blob under a `matrix` key). Store additions: `getMatrix()`, `updateMatrixCell(service, path[], value)` (document=`[content,tier,size]`, vellum=`[tier,size]`, sticker=`[tier]`, photo=`[size,'price'|'minQty']`), `setMatrix()`, `resetPricing()` now also resets the matrix; `getPriceFromMatrix(matrix, service, {contentType,colorTier,sizeKey,photoSize,field})` + `resolveColorTier(colorMode, colorPct)` + `mapPaperSizeKey(paperSize)` (shortâ†’short; long/legal/folioâ†’long; a4/letter/a3/unknownâ†’a4) lookup helpers; label maps `COLOR_TIER_LABELS`/`CONTENT_TYPE_LABELS`/`PAPER_SIZE_LABELS`/`SERVICE_TYPE_LABELS`/`PHOTO_SIZE_LABELS` + short `SERVICE_TAB_LABELS` (Document/Vellum/Sticker/Photo) for the admin tab row. `PricingManagement.tsx` rebuilt: tabs per service type (Document = 3 content-type card-grids by colorÃ—size; Vellum = colorÃ—size table; Sticker = per-sheet rows; Photo = price + min order per size) + a "Legacy Per-Page Rates" section for the old flat fields. PHASE 2 (order-flow adoption) DONE: customer NewPrintRequest.tsx and shared UnifiedWalkInTransactions.tsx now price via getPriceFromMatrix (NOT legacy calcPagePrice). Both got a Service Type selector grid (Document/Vellum/Sticker/Photo) with a Photo config panel (Photo Size + Matte/Glossy finish + Quantity with min-order note), a per-file Content Type dropdown (text/textWithImage/imageOnly, auto-detected as imageOnly for image uploads, editable), and calculateFileTotal computes one color tier per file via resolveColorTier(colorMode, avgColoredPct) + mapPaperSizeKey(paperSize) then applies the matrix per-page rate x pages x copies (div pages-per-sheet). calculateTotal/summary branch to the photo total when serviceType==='photo'. Photo has no files: Next Step jumps 1->4, Back 4->1, and Next/Proceed skip the must-upload gate for photo. handleSubmit/handleProceedToQueue build a photo order object (serviceType, photoSize/photoFinish/photoQty, copies=photoQty, type=Photo) with min-qty enforcement and reset service/photo state. Legacy per-page color labels (color-mode radio, See Colored Pricing Breakdown) still show the flat legacy values display-only; actual cost uses the matrix. Build passes.
- PAYMENT METHODS & QR OVERHAUL (dynamic, admin-managed): online payment methods are NO LONGER hardcoded to GCash-only. New `src/app/utils/paymentMethodsStore.ts` (localStorage `docufy_payment_methods_v1` + version key; seeded GCash + Maya with placeholder SVG QRs; `PaymentMethodType` = id/name/accountName/accountNumber/qrCode(dataURL)/active/createdAt/updatedAt; API: getAllPaymentMethods/getPaymentMethods(active)/findById/findByName/nameExists/addPaymentMethod/updatePaymentMethod/setActive/deletePaymentMethod/subscribe + helpers `downloadQRCode(qr, filename)` (anchor-click data-URL download) and `methodQRFilename(method)`; mirrors the inventoryStore pattern). New shared `src/app/components/shared/PaymentMethodQR.tsx` â†’ `PaymentMethodQRPanel({method})`: white bordered card, QR image (`w-40 h-40`), **Download QR Code** button, Account Name / Account Number / Payment Method rows, "Scan this QR code..." note â€” reused on the customer payment page, admin/staff verification detail dialog, NewPrintRequest View QR modal. "Cash on Pickup" stays a built-in special (not in the store). Legacy `admin/PaymentVerificationAdmin.tsx` + `staff/PaymentVerification.tsx` files are DEAD (not routed) and keep hardcoded GCash; do not edit.
- NEW ADMIN PAGE `src/app/components/admin/PaymentMethodsManagement.tsx` at `/admin/payment-methods` (ProtectedRoute admin): header + Add Payment Method button; 3 summary cards (Total/Active for Customers/Inactive); responsive card grid (QR thumbnail + name + account details + Active/Inactive badge + Download QR + Edit/Deactivate/Activate/Delete). Add/Edit dialog fields = Payment Method Name* (unique), Account Name/Account Holder*, Account Number/Payment Account*, QR Code upload (image only PNG/JPG/WebP/SVG/GIF, <3MB, FileReaderâ†’dataURL preview + remove; OPTIONAL â€” placeholder auto-generated if skipped). Toasts: created/updated/activated/deactivated/removed. Delete uses a lightweight confirm dialog (NOT ConfirmationDialog). Added "Payment Methods" (QrCode icon) to `adminMenuItems.tsx` right after Payment Verification (admin MobileNavSheet inherits) + route in App.tsx.
- FLOW: admin adds/edits/deactivates â†’ customers instantly see it (NewPrintRequest radio options + View QR modal; customer `customer/PaymentVerification.tsx` method selector + PaymentMethodQRPanel with Download) â†’ order stores the METHOD DISPLAY NAME in `order.paymentMethod` (e.g. "GCash"/"Maya"/"Cash") â†’ admin/staff verify (`shared/UnifiedPaymentVerification.tsx`) where the detail dialog now shows the corresponding method badge + full QR panel (name/account/QR + Download QR Code). `UnifiedPaymentVerification` `PaymentType.method` widened `"GCash"|"Cash"` â†’ `string`; method resolved from `order.paymentMethod` with legacy fallback (`walkin ? 'Cash' : 'GCash'`); reference label dynamically `${method} Reference Number`; subscribes to paymentMethodsStore so QR edits re-render live. Deactivated methods show a red "no longer available â€” change payment method" notice on the customer payment page (kept out of active lists but still resolvable for history).
- CUSTOMER SIDE REWIRES: `customer/PaymentVerification.tsx` fully rewritten â€” `paymentMethod` state = method name or "cash"; live store subscription; selector maps active methods (Smartphone chip) + Cash; online shows PaymentMethodQRPanel (QR + Download) instead of the old hardcoded GCash box; reference-validation/scan copy uses the method name; deactivated-method warning banner. `NewPrintRequest.tsx`: removed `gcashAccount` state entirely; online method list from store subscription; `isOnline = paymentMethod !== "" && paymentMethod !== "cash"`; order `paymentMethod` = method name or "Cash"; holdReason/notifications/toasts use the method name ("{Method} payment of â‚±X is pending verification..."); "View QR" inline button per online option opens a PaymentMethodQRPanel dialog; success modal / down-payment copy generalized ("pay in full via your chosen payment method").
- COPY GENERALIZATION: OrderTracking Awaiting-Payment card (holdReason fallback + verify text) no longer says "GCash"; LandingPage + SignUpPage terms say "online payment methods or Cash on Pickup".
- REBRAND (DocuFy â†’ Docufy, whole product): applied via node script `C:\Users\agopr\AppData\Local\Temp\opencode\docufy-rename.js` (split/join on "DocuFy", skip COLLABORATION_GUIDE.md, exts ts/tsx/js/jsx/md/html/css) across 25 files incl. all components, README, PUSH_LOG, AGENTS.md, `confirmation-dialog.tsx` (ConfirmationDialog now expects "Docufy"). Deliberately untouched: `https://github.com/pranswg/DocuFy-prot.git` (collab repo URL in COLLABORATION_GUIDE.md), already-lowercase `docufy.com` emails, `docufy_*` localStorage keys, `docufy-psms` package name, `docufypsms` index.html title. Capstone copy (`...\capstone\DocuFy-prot`) still reads "DocuFy".
- Auth context, role-based routing, ProtectedRoute, shared Layout (sidebar+header, notifications, SIEM alerts for admin).
- STAFF MANAGEMENT OVERHAUL (admin/Staff.tsx rewritten): replaced the card grid + large tabbed detail dialog with a clean, professional data TABLE â€” columns Staff (blue initials avatar + name + EMP-id), Email, Role (Admin = blue-tinted Shield badge / Staff = gray User badge), Account Status (Active green dot / Inactive red dot badge), Date Added (formatted), Actions (Edit icon + Activate/Deactivate icon w/ hover tints). Search (name/email/ID) + status filter, empty state, footer summary (Showing X of Y Â· N active Â· N inactive). "+ Add Staff" opens an ADD dialog (Full Name, Email, Password + Confirm with show/hide toggles + PasswordStrengthIndicator, NEW Role select Staff/Admin, mobile h-11 buttons) â€” validation for required fields, email format, password strength, mismatch, duplicate email (checks staff list + auth mockUsers via registerStaff). SUCCESS TOAST = "Staff account created successfully". Since the add flow uses AuthContext, a REAL login account (email/password, in-memory mockUsers) is created so the new staff can actually sign in. EDIT dialog (Full Name, Email, Role Staff/Admin, Account Status Active/Inactive). Activate/Deactivate go through ConfirmationDialog and reflect in AuthContext so a deactivated account is BLOCKED at login.
- AuthContext upgrades (`src/app/contexts/AuthContext.tsx`): `User` gained `active?: boolean` (mockUsers + signup + registerStaff all set `active: true`); `login` returns `{ success:false, reason:'inactive' }` when the account is deactivated (LoginPage shows "This account has been deactivated. Please contact your administrator." instead of "Invalid email or password"); `registerStaff` signature now `{ name, email, password, role?: 'staff' | 'admin' }` (role persisted on the mock user; internal success toast removed so the caller controls messaging); NEW `updateStaffAccount(currentEmail, { email?, name?, role?, active? })` updates a mockUsers record in place (returns false if the current email has no auth account â€” e.g. seeded roster entries Robert Chen/Katie Perry are directory-only rows with no login, edited via the fallback toast "Staff details updated successfully").
- Customer: dashboard, new print request, orders, order tracking, payment, job board + apply. Job board reads from jobsStore.
- My Orders page: mobile back button moves out of the header into the page Top Action Bar (left of Print Request, same row). Layout gained `hideMobileBackButton` prop.
- My Orders mobile filters "See More/Show Less" toggle (see `See More / Show Less` block in CustomerOrders.tsx): Show More fields (Date From/To, Search, Clear) live in a grid drawer that animates `grid-template-rows` 0fr->1fr over 300ms on mobile (`sm:contents` wrappers keep the flat desktop flex-row layout), so the fields expand smoothly downward and push the button below them. Button = filled blue `#2F6FD6` when "See More" (collapsed), white with a `2px` blue border when "Show Less" (expanded). Drawer gets `-my-2` when collapsed so the double gap (Status toggle -> 0-height drawer -> button) doesn't add too much spacing.
- Job Board listings: when expanded, gap between the location/meta row and the details is fully removed â€” Card gains `gap-0` (base Card is `flex flex-col gap-6`), header container `pb-0`, details section `pt-0`.
- Staff/Admin dashboards with KPI cards.
- STATUS CARD COLORS (UnifiedOrders.tsx summary cards): each status card uses a selected / unselected fill pair (arbitrary Tailwind values) â€” Received #4A5568/#EDF2F7, In Queue #D69E2E/#FEFCBF, Printing #3182CE/#EBF8FF, Completed #38A169/#C6F6D5, On Hold #DD6B20/#FEEBC8, Released #0D9488/#CCFBF1, Canceled #718096/#E2E8F0. Selected = accent fill + white label/value + white/15 icon chip; unselected = light fill + white/60 chip with accent icon, hover border = accent. "All Orders" card keeps its blue style.
- GCASH ORDER PAYMENT GATE: online GCash orders are created with status 'Awaiting Payment' (dataStore) / 'awaitingPayment' (ordersStore) â€” NOT received/in-queue â€” so they do NOT appear in the Orders/queue list or any status KPI (getOrderStats counts only explicit statuses). They show in Payment Verification as Pending until admin/staff verifies (UnifiedPaymentVerification); verification sets status to 'Received' so the order then enters the queue. UnifiedOrders keeps `orders` state full (setOrders write-back preserves awaitingPayment) but derives a `queueOrders` memo (excludes 'awaitingPayment') used for the table, search/status filters, queue-order validation/positions, and the "All Orders" count. NewPrintRequest: GCash â†’ 'Awaiting Payment'; cash â†’ 'Received', or 'Awaiting Payment' when a down payment is required (total â‰¥ â‚±50 DOWN_PAYMENT_THRESHOLD) â€” downpayment-requiring orders are now treated EXACTLY like GCash: they use 'Awaiting Payment' (NOT 'On Hold'), so `queueOrders` (excludes 'awaitingPayment') keeps them OUT of the queue/KPIs and they appear in Payment Verification as Pending until verified. UnifiedPaymentVerification.generatePaymentsFromOrders no longer auto-verifies Cash orders that require an unverified down payment (they stay Pending); approving sets `downPaymentVerified: true` + status 'Received' so the order then enters the queue. ordersStore.convertToDataStore/convertFromDataStore now round-trip `paymentMethod` so online cash orders aren't misclassified as GCash/pending. CustomerOrders + CustomerDashboard added an 'Awaiting Payment' badge (yellow) + status filter option; OrderTracking adds an "Awaiting Payment" amber alert card, cancel eligibility (Received/In Queue/On Hold/Awaiting Payment), and badge/icon handling.
- ATTENDANCE SYSTEM REMOVED (whole feature): deleted `src/app/utils/attendanceStore.ts` and `src/app/components/AttendanceWidget.tsx`. Removed the widget from both dashboards (AdminDashboard.tsx, StaffDashboard.tsx) so the "Quick Actions" card now sits at the top of each right sidebar where Attendance was (staff side keeps `lg:col-span-3 flex flex-col pt-[68px]` with the `flex-1` card; admin side `space-y-6` now has only the Quick Actions card). Removed the admin Staff detail dialog's Attendance tab (tabs now Profile/Access, `grid-cols-2`), the `attendanceLogs` type field + mock data + `attendanceLogs: []` on new staff, and the header/description text. NewPrintRequest.tsx lost its dead `availability` state and attendanceStore subscription (value was never rendered) plus the store import. No attendance references remain in src except one mock performance-note string ("Good attendance and punctuality") in Staff.tsx, which was left untouched.
- INVENTORY SYSTEM (REBUILT from scratch): admin-only `src/app/components/admin/InventoryManagement.tsx` at `/admin/inventory`, backed by new `src/app/utils/inventoryStore.ts` (localStorage, `inventoryStoreVersion` = '2.0' â€” reseeds; keeps only 1 mock item per sample: paper A4/Short/Legal + 1 ink + 1 school supply + 1 add-on). Item fields: name, category (Paper/Ink/School supplies/Add-ons/Other), brand (optional), unit, currentStock, minimumStock, price (sell price, Add-ons only), paperSize (size code, Paper only), pcsPerUnit (pieces per unit â€” paper ream = 500; defaults 1). Actions: Add/Edit, Archive/Restore, Stock In/Out (Stock Out never below 0). PAPER TRACKING: a "Papers Left" card on the page shows total paper pieces left (Î£ currentStock Ã— pcsPerUnit; 1 ream = 500 pcs) plus per-item piece counts; the table/dialog also show "(N pcs)" for paper rows. ORDER-BASED DEDUCTION: NewPrintRequest `handleSubmit` aggregates sheets used per file (`ceil(pageCount/pagesPerSheet) Ã— copies`) by paperSize and calls `inventoryStore.deductPaperPieces(size, pieces)` (reduces the matching Paper item's currentStock by pieces/500, flooring at 0), so placing an order automatically reduces paper on hand. Low-stock = currentStock <= minimumStock; amber banner + row badges (red "Out of Stock" at 0). Active/Archived toggle, search, summary cards. Added Inventory to `adminMenuItems` (Boxes icon; admin mobile sheet inherits) + `/admin/inventory` ProtectedRoute in App.tsx. NewPrintRequest loads paper-size options from `inventoryStore.getPaperSizeOptions()` (A4/Short/Legal/A3, disabled + '(Out of Stock)' when 0) and add-ons from `inventoryStore.getAddons()` (Add-ons items, priced by item.price) via a subscribe effect, propagating admin changes live. NOTE: only PAPER is auto-deducted on order placement; other materials are managed manually via Stock In/Out. CRASH FIX: the page previously threw "Cannot read properties of undefined (reading 'toLocaleString')" at formatNumber (formatNumber.ts:8) from InventoryManagement.tsx:713 â€” the Stock In/Out <Dialog> reads `stockDialog?.item.*` and its <DialogContent> rendered the body even when `stockDialog` was null, so `formatNumber(undefined)` blew up. Fixed by wrapping the Stock dialog body in `{stockDialog && (<>...</>)}` so nothing dereferences `stockDialog.item` when closed. Hardened formatNumber.ts to coerce `null`/`undefined` â†’ `0` (defensive). PAPER PCS DISPLAY: the table Stock column and the Stock In/Out dialog now show the PIECE count as the primary value for Paper items (e.g. `2,500 pcs (5.00 reams)`) instead of the ream/unit count â€” `currentStock Ã— pcsPerUnit` (1 ream = 500 pcs) is the headline figure, with the unit count as secondary. PCS PER UNIT ON SAVE/MIGRATION: a manually-added Paper item previously saved without `pcsPerUnit`, so `getItemPieces` treated it as 1 (5 reams showed "5 pcs" instead of "2,500 pcs"). Fix: `saveItem` now sets `pcsPerUnit = 500` for Paper items whose unit is `ream` (else 1, preserving an existing edit value) on add AND update; store `normalizeItem()` backfills `pcsPerUnit` for any stored Paper item missing it (ream â†’ 500, else 1), applied on load.
- INVENTORY LOW/OUT STOCK ALERTS (staff/admin): new `src/app/utils/inventoryAlerts.ts` (module-level side effect, imported in App.tsx) subscribes to `inventoryStore` and emits a system notification when any item drops to/below its minimum stock (`priority: 'important'`, amber "Low Stock" alert) or runs fully out of stock (red "Out of Stock", `priority: 'emergency'`). Alerts go to Staff/Admin ONLY (`notificationStore` gained `type: 'inventory'` + `priority` + `recipientRole: 'staff_admin'`); dedup via localStorage `docufy_inventory_alert_state_v1` (fires once per item per status, promoted to emergency on full out). NEW read-only STAFF Inventory page `src/app/components/staff/StaffInventory.tsx` at `/staff/inventory` (protected; summary cards, paper-pieces card, All/Low/Out filter chips, table via shared `inventoryStore.getInventoryStatus`/`getItemPieces`) — added to staff nav in MobileNavSheet + StaffOrdersUnified/StaffPaymentVerificationUnified/StaffDashboard/StaffTimesheet/StaffProfile/UnifiedWalkInTransactions/NotificationsPage/StaffInventory. `notificationStore` already had type/push/priority/recipientRole (`SystemNotification` gained `type: 'inventory'` + `priority`); NotificationsPage `renderSystemCard` is now priority-aware (emergency red `AlertOctagon` + left-red-border, important amber `AlertTriangle` + amber border) with a `View Inventory` button (`stopPropagation` + role-aware `openSystemNotification` → `/staff/inventory` or `/admin/inventory`). Layout `getNotificationIcon(type, priority)` returns role-aware unreadDot (red/amber) + routes inventory bell click role-aware. AdminDashboard `InventorySnapshot` uses shared `getInventoryStatus("low"/"out")` for consistency. `inventoryStore` refactored: `InventoryStatus`, `getInventoryStatus(item)` (out → low → ok), `getOutOfStockItems`, `isOutOfStock`; `isLowStock`/`getLowStockItems` now compare `getInventoryStatus === "low"`. Build passes.
- Staff Dashboard Active Orders box (StaffDashboard.tsx): removed the "Newest updates first Â· Live from Orders page" subtitle and the Received/In Queue/Printing/On Hold mini status pills from the card header (kept title + "View Full Queue" button and the table/footer). Removed the now-unused `getStatusIcon` helper and unused lucide imports (FileText, Printer, LogIn, LogOut).
- Jobs: localStorage-persisted via jobsStore (seeded w/ 3 defaults). Admin can add/archive/restore; shows for customers. Fixed status casing (active) + id assignment.
- Portfolio PDF/image preview: NO inline viewer at all. Both admin (JobBoardManagement.tsx) and customer (JobBoard.tsx) open portfolio files in the browser's NATIVE PDF viewer in a NEW TAB via a module-level `openBlobInNewTab(blob)` helper: `URL.createObjectURL(blob)` -> `window.open(url, "_blank", "noopener,noreferrer")` -> delayed `URL.revokeObjectURL(url)` (60s, after tab load) to avoid leaks/blank viewer; anchor-click fallback if popup blocked; external links via `<a target=_blank>` / `window.open`. Shared `src/app/components/shared/PortfolioPreview.tsx` (was the pdf.js viewer) DELETED. Customer file-view button labeled "View". Admin's redundant "View Portfolio" modal dialog (showResumeDialog) fully REMOVED â€” the View Portfolio / Open Link buttons only open the new tab, no modal.
- FILE/PRINT PREVIEW REMOVED SYSTEM-WIDE (pending rebuild): deleted `src/app/components/ui/print-preview-dialog.tsx` (the pdfjs-dist live document preview dialog) and removed the `pdfjs-dist` dependency (from package.json + lockfile), which also dropped the ~1.2MB pdf.worker chunk. Rewrote `src/app/components/ui/file-attachments.tsx` to remove its local preview windows (`DocumentPreview` + `FilePreviewModal` simulated previews) â€” the file list, download button, and an inert "View" button remain (`View` calls `onView && onView(file)`, so consumers without an `onView` handler get a no-op). Inerted every remaining preview trigger and removed their dialog blocks/imports/state: NewPrintRequest.tsx (Preview button inert, `title="Preview is currently unavailable"`), OrderTracking.tsx, UnifiedOrders.tsx, UnifiedWalkInTransactions.tsx. Result: no print/file preview window opens anywhere â€” buttons are visible but do nothing. Other "Preview" features (invoice preview, payment-proof image preview, ContentManagement landing preview) are UNRELATED and untouched. NOTE: pdfjs-dist may be a transitive dep that got re-pruned by npm install; rebuild the preview feature later without pdfjs.
- Logo .png imports: added src/vite-env.d.ts (/// vite/client) to clear TS "cannot find module" squiggles.
- KPI cards unified to admin style on desktop only (md:) for customer; mobile keeps blue compact style.
- Customer dashboard order: KPI grid on top, quick actions+recent orders below, info cards last. (Reverted a swap back to this.)
- SYSTEM-WIDE BLUE-WHITE THEME (semantic colors preserved): flattened all non-blue accents to the blue/white/gray system palette â€” purple â†’ blue, orange â†’ blue, pink/cyan â†’ blue, and all NON-verified/check greens â†’ blue. KEPT green ONLY for: check glyph icons (Check/CheckCircle/CheckCircle2 `text-green-*`) and positive/verified semantic states (password 'Strong' + requirements-met, 'Verified' badge, task 'completed', down-payment Verified). KEPT red (errors/Rejected/delete), amber/yellow (warnings/Pending). Brand blues: `#1D73EC` (customer primary) and `#2F6FD6` (admin primary) both retained (near-identical system blue). Applied mechanically across all admin/customer/staff components (~30 files).
- SYSTEM-WIDE KPI-CARD BUTTON HOVER (desktop): every NON-destructive button now has the KPI-card hover â€” WHITE base with a LIGHT BLUE OUTLINE that on hover fills blue with white text, slight lift + shadow (`hover:bg-[#2F6FD6] hover:text-white transition-all hover:-translate-y-0.5 hover:shadow-md`). Implemented at two levels: (1) central `src/app/components/ui/button.tsx` â€” the `default`/`outline`/`secondary` variants use a white base with a light blue outline `border-2 border-blue-200` (`bg-white text-slate-700 border-2 border-blue-200 hover:bg-[#2F6FD6] hover:text-white hover:-translate-y-0.5 hover:shadow-md`; `ghost` keeps just the hover) so a white button still reads as a button (per user: light blue outline like the Documents Analysis box, NOT a shadow, NOT a solid blue border); (2) the ~40 inline filled-blue overrides (`bg-[#1D73EC] hover:bg-[#10316B]`, `bg-[#2F6FD6] hover:bg-[#2557b8]`/`[#1e5bb8]`, `bg-[#2F6FD6] ... hover:bg-[#2F6FD6]/90`) on `<Button>`s across ~25 auth/public/admin/customer/staff/shared files were swapped to the white-base KPI pattern with a LIGHT BLUE OUTLINE (`bg-white text-[#1D73EC]/[#2F6FD6] border-2 border-blue-200 hover:bg-[#1D73EC]/[#2F6FD6] hover:text-white`), plus the 3 upload `<span>` labels (NewPrintRequest/WalkIn/PaymentVerification) and the Inventory primary actions (Add Item/Save). "See Colored Pricing Breakdown" (NewPrintRequest) themed via `variant="outline"` + inline color. UPLOADED FILE BOXES: the per-file cards in the print-request/walk-in uploaded-files list (NewPrintRequest ~789 / UnifiedWalkIn ~600) changed from blue (`border-blue-200`) to a GRAY outline `bg-white border-gray-300` (matching the gray dashed Add More Files box). REVERTED to old filled-blue (kept `bg-[#2F6FD6] text-white hover:bg-[#2557b8]`): the print-request steel flow "Next Step"/"Place Order" (NewPrintRequest ~2190/2207) and WalkIn "Next Step"/"Proceed to In Queue" (~1597/1613). EXCLUDED (kept as-is, per user): destructive/red (cancel order, confirm cancellation, reject, delete, sign out, remove photo, SIEM mark-all-read), amber warning (Verify Down Payment, Archive), green positive (Attendance Complete, verified/Stock In), status-chip toggles (UnifiedOrders/StaffQueueBoard Receivedâ†’Cancel selected fills), filter/tab/stepper active-state fills (Active/Archived, JobBoard tabs, See More/Show Less, stepper circles), stateful Clock-In/Time-In CTAs (StaffTimeInGate/StaffTimesheet green-complete/dark-timeout/blue-timein ternary), sidebar/nav/dropdown items, MFA/switch toggles, decorative blue cards/avatar circles/camera labels. Build passes.
- SYSTEM-WIDE POINTER CURSOR (desktop hover): Tailwind v4 defaults buttons to `cursor: default`, so added a global base rule in `src/styles/tailwind.css`: all native `button` and `role="button"` (non-disabled) show `cursor: pointer`. Shared `Card` component (`src/app/components/ui/card.tsx`) auto-adds `cursor-pointer` whenever a `<Card>` has an `onClick`. `data-cursor-default` attribute = opt-out keeps default cursor. Backdrop scrims (invisible click-to-close overlays in Layout, file-attachments) intentionally stay default cursor.
- MOBILE SIDEBAR NAV TRANSITION: the mobile nav Sheet is now a PERSISTENT sibling of the router (<code>MobileNavSheet</code> in App.tsx, rendered inside <code>MobileNavProvider</code> but OUTSIDE <code>RouterProvider</code>, given the router object via prop) so it survives route changes. It uses <code>useSyncExternalStore(router.subscribe, ...)</code> for active-link highlighting, its own per-role menu arrays (customer/staff/admin, admin reuses adminMenuItems), full Layout styling (logo, collapse button, profile dropdown/logout), and its own open state stored in <code>MobileNavContext</code>. Layout.tsx no longer renders a Sheet â€” its mobile hamburger toggles the context instead. On nav-item click, <code>navigateAndClose</code> calls <code>setOpen(false)</code> + <code>router.navigate(path)</code> IMMEDIATELY (no delay), so the next page mounts at once while the sheet plays its 300ms slide-out collision animation on top. Applies to all roles.
- DROPDOWN OPEN/CLOSE ANIMATIONS: custom dropdown menus in Layout.tsx (desktop sidebar profile, top-right profile, notifications, SIEM alerts) and MobileNavSheet profile use a shared `usePresence(open, 200)` hook (`src/app/components/ui/use-presence.ts`) that keeps the panel mounted for 200ms after close so exit CSS can play, pair it with tw-animate-css entrance classes (`animate-in fade-in-0 zoom-in-95 slide-in-from-(bottom|top)-2 duration-200`) and exit classes (`animate-out fade-out-0 zoom-out-95 slide-out-to-(bottom|top)-2 duration-200`, `pointer-events-none` while closing; backdrop scrims fade with `fade-out-0`). Closes mirror the opens exactly: the `-2` spacing suffix makes the exit slide the same 8px as the enter (plain `slide-out-to-top`/`-bottom` would slide 100% and look wrong).
- STAFF TIME-IN LOCKOUT (same as capstone copy): new `src/app/components/shared/StaffTimeInGate.tsx` â€” blurred-backdrop Time-In modal â€” wraps the Layout children of the four staff function pages (shared/UnifiedOrders, shared/UnifiedPaymentVerification, shared/UnifiedWalkInTransactions, staff/InventoryManagement â€” the last was DELETED in the remote merge, so the gate now wraps three pages). Staff are locked unless `attendanceStore.getNextAction()` is an active on-clock state; Time In (Morning/Afternoon) via the modal unlocks. RE-ADDED `src/app/utils/attendanceStore.ts` (Morning/Afternoon session model) as the lockout backbone only â€” the attendance UI/dashboard widget stays REMOVED, and the gate's secondary action is labeled "Open Clock-In & Timesheet" (navigates to /staff/timesheet).
- STAFF REGISTRATION (same as capstone copy): new `registerStaff({name,email,password})` in `src/app/contexts/AuthContext.tsx` (pushes a role:'staff' mock user so they can sign in). Admin `Staff.tsx` "Add Staff" dialog rewritten to **Register New Staff** dialog (Full Name / Email Address / Password / Confirm, show-hide toggles, PasswordStrengthIndicator, Start Date, info note) with mobile-responsive `h-11`/`h-12` full-width buttons and responsive header action row.
- STAFF CLOCK-IN & TIMESHEET MODULE (Worksy concept): new `src/app/components/staff/StaffTimesheet.tsx` at `/staff/timesheet` (protected, NOT wrapped in StaffTimeInGate so staff can clock in without navigating to a gated page; staff dashboard also stays ungated).
  - **Clock-In widget**: Card with live date header + status Badge (Clocked In green / Shift Complete green / Not Started gray / On Break amber). Big mono timer â€” session elapsed HH:MM:SS (blue) while on clock, otherwise wall clock (slate). Single `h-14` full-width CTA: Time In (`bg-[#1D73EC]`), Time Out (`bg-[#1c1f26] text-white`), or "Attendance Complete for Today" (green `disabled:opacity-100`). Below it, "Back to Dashboard" link. Time In/Out via `attendanceStore.timeIn/timeOut` (Morning/Afternoon) with sonner toasts.
  - **Metrics**: three cards â€” Total Hours, Break Time, Overtime â€” each showing Today (big) + This week (sub). Break = gap between Morning time-out and Afternoon time-in. Overtime = max(0, total âˆ’ 8h/day or âˆ’ 40h/week). All tick live via a 1s `setInterval` + `attendanceStore.subscribe`.
  - **Personal Time Logs**: collapsible history card; collapsed shows today summary, expanded shows full table (Date | Morning In/Out | Afternoon In/Out | Break | Total | Overtime, Today badge + blue highlight row while on clock, empty state). Footnote: demo persistence + standard shift note.
- STORE UPGRADES (attendanceStore.ts): records PERSIST to `localStorage` key `docufy_attendance_records_v1` (ISO-string serialize/restore, `persist()` in `notify()`); local-day `toDateKey`. Analytics exports: `STANDARD_DAILY_HOURS = 8`, `STANDARD_WEEKLY_HOURS = 40`, `getWeekStartKey()` (Monday-based), `sessionTotalMs(record, until)`, `sessionBreakMs(record)`, `overtimeMs(totalMs, standardHours)`, `getWeekRecords(userId)`, `getRecord(userId, date)`. ADMIN MONITORING additions: `upsertSession(userId, userName, role, date, session, field, value|null)` â€” manual timestamp adjust (creates record on first use; clearing timeIn clears that session's timeOut); separate ABSENCE map persisted to `docufy_attendance_absences_v1` with `setAbsence(userId, date, 'on-leave'|'absent'|null)` and `getAbsence(userId, date)` (does NOT affect staff NextAction/lockout).
- ADMIN ATTENDANCE & STAFF MONITORING VIEW: new `src/app/components/admin/AdminAttendance.tsx` at `/admin/attendance` (ProtectedRoute admin) â€” PASSIVE monitoring dashboard (no timer UI). Added to `adminMenuItems.tsx` (after Staff; mobile nav + walk-in admin menus inherit automatically).
  - **Tabs** (ui/tabs): Attendance / Overtime / Time Off / Work Time â€” each filters the same row set (overtime rows / absence!=null / present rows), table re-renders per tab.
  - **KPI cards** (AdminDashboard style, hover-blue): Total Staff, Present (`n on time Â· n late`), Non-Present (`n absent Â· n no clock-in`), Away (On Leave); green/red/amber icon accents; computed for TODAY regardless of date filter; header pulse line shows `x staff currently on clock`.
  - **Table** (`overflow-x-auto`, Staff.tsx-style gray th/td): Staff Name (blue initials avatar + email), Role (position), Clock-In, Clock-Out (shows green "On Clock" when a session is still open), Hours Rendered (h/m), Status badges (On Time green / Late amber / Overtime blue; presence pills On Leave amber / Absent red / No Clock-In gray), per-row action dropdown (Adjust Clock-In, Adjust Clock-Out, Mark/Clear On Leave, Mark/Clear Absent, Reset Day's Record). Adjust opens a dialog (Morning/Afternoon session select + datetime-local) writing through `attendancesStore.upsertSession` so staff timesheets reflect edits.
  - **Filters**: name/email search, From/To date inputs (single day = roster snapshot for all staff; multi-day = history rows from `getAllLogs()` with a Date column), status-badge Select, Reset button. Status derivation: On Time cutoff 8:30 AM (`LATE_CUTOFF`), Overtime > `STANDARD_DAILY_HOURS` (8h), presence = present/no-clock-in/absent/on-leave from records + absence map. Live 30s tick + `attendanceStore.subscribe` re-render.
  - **Roster**: new `src/app/utils/staffRoster.ts` â€” `SEEDED_STAFF` (Heaven Rica, Robert Chen, Katie Perry + Miguel Santos, Ana Dela Cruz) merged with any staff seen in attendanceStore logs; `getStaffRoster()` + `seedDemoAttendance()` (idempotent, TODAY only: Robert On Time 8h, Katie Late 9:42, Miguel Overtime 10h05m, Heaven On Leave, Ana Absent; never touches staff@test.com clock flow).
- Nav: Timesheet appears in staff menus as Clock-In & Timesheet after Dashboard in MobileNavSheet, StaffDashboard (also a "Clock In / Time Out" Quick Action), StaffOrdersUnified, StaffPaymentVerificationUnified, UnifiedWalkInTransactions.staffMenuItems, StaffProfile, and StaffTimesheet's own menuItems.
- MERGE WITH REMOTE (2026-08-30, commit 150eff71): pulled Francis' `testbranch2` commits â€” Reports feature removed (Reports.tsx/ReportsView.tsx, /admin/reports route + menu, docs, README rework); inventory rebuilt ADMIN-ONLY (new `InventoryManagement.tsx` + v2 `inventoryStore` with paper-piece tracking + down-payment gating in UnifiedPaymentVerification); `staff/InventoryManagement.tsx` and `utils/inventoryIntegration.ts` DELETED â€” all dead `/staff/inventory` nav links and the `inventoryIntegration` import (StaffQueueBoard) removed on both sides; `COLLABORATION_GUIDE.md` added (workflow: pull â†’ commit â†’ pull/merge â†’ push on testbranch2). Post-merge build passes; zero references to `inventoryIntegration` / `/staff/inventory`.
- ORDER STATUS COLOR SYSTEM (cohesive pastel palette): new `src/app/utils/orderStatusPalette.ts` â€” `ORDER_STATUS_STYLES` (bg = soft pastel, accent/hover = stronger color for borders, chip = tinted icon circle, icon, label, badge classes) + `getOrderStatusStyle()` / `getStatusBadgeClasses()`. Palette = All Orders #EAF3FF/#3B82F6, Received #F1F3F5/#6B7280 (grey), In Queue #FFF5D6/#F59E0B, Printing #F0EAFE/#7C3AED, Completed #E8F7D8/#55A630 (apple green, clearly success), On Hold #FFF0E6/#F97316, Released #E0F7F5/#159A9C (teal, distinct from Completed & NOT green), Canceled #FDE8E8/#DC2626, plus awaitingPayment #FEF3C7/#F59E0B (badge-only, not a summary card). The 8 UnifiedOrders.tsx summary cards are STAT CARDS: plain-div, 4Ã—2 grid that collapses to 2 then 1 col (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`), `rounded-[22px]`, `shadow-sm` (â†’`shadow-md` on hover), white bg + `border-2 border-gray-100` OUTLINE at rest that COLORIZES on hover / selected state (`hover:bg-[#pastel]` + `hover:border-[#accent]` via `hoverBg`/`hover` fields; selected pins `bg` + `border-[#accent]` + `shadow-md`). Left circular tinted icon chip (`w-10 h-10 rounded-full`, always accent-tinted), medium-weight accent-colored label to its right, large `text-3xl` bold count and small accent-tinted description text both indented `pl-[52px]` to align with the label. Descriptions: Total orders / Orders received / Waiting to be printed / Currently printing / Successfully completed / Temporarily on hold / Ready for pickup / Canceled orders. Keeps click-to-filter (toggles statusFilter) but reads as a passive stat card (no border all-around, arrow cursor). Table/detail status badges + customer pages use the same palette via getStatusBadgeClasses/getStatusColor (CustomerOrders, CustomerDashboard, OrderTracking banner+details+invoice). Legacy AdminOrders.tsx/StaffQueueBoard.tsx deleted (now unrouted legacy files are fully removed). Literal Tailwind class strings so v4 scanner picks them up (verified in dist CSS).
- LOCAL MERGE (2026-08-30, on testbranch2, NO PUSH): fetched collaborator's `testbranch2` latest (d89aa5fc, 3 commits â€” staff clock-in/registration via 322e5a24 + admin attendance monitoring, merge commit 150eff71) and fast-forwarded local testbranch2 onto it. Then merged my PDF-preview-removal branch `testbranch-pdf` (40a2c710) into local testbranch2 â€” this combined the collaborator's ATTENDANCE feature with the FILE/PRINT PREVIEW REMOVAL (see the "FILE/PRINT PREVIEW REMOVED SYSTEM-WIDE" note above) while KEEPING the inventory system. Clean auto-merge (0 conflicts; the two sides edited different sections of AGENTS.md / UnifiedOrders.tsx / UnifiedWalkInTransactions.tsx). `pdfjs-dist` removed from package.json+lockfile and pruned from node_modules; `print-preview-dialog.tsx` deleted; preview buttons inert. Build passes; pdf.worker chunk gone. `testbranch-pdf` left intact for later deletion (disregard).

### Pending / Notes
- PHASE B (OMITTED — NOT to be implemented for now, per user "omit the automated ping/warning muna, only manual toggle"): Supabase heartbeat / auto-outage-detection / any automated warning. Manual toggle ONLY (Phase A above). If it is picked up later, the settled design from the brownout discussion was: shop PC hearts a single-row `shop_status` table (30s is fine cost-wise; the real cost is customer polling, so: customers fetch once on load, compute staleness locally from `lastSeen`, live updates via Supabase Realtime push).
- Supabase integration NOT in place: jobs = localStorage (per-browser); portfolio File objects are in-memory only (no cross-session sharing; admin preview needs File present in session). Real multi-device sharing requires backend.
- apps not persisted across sessions; portfolio preview relies on same-session File object.
- "Today's Changes" PDF (Desktop\08092026 Docufy Frontend Changes.pdf) — generator script at Temp\opencode\mk_changes_pdf.ps1 was fixed (blank-page bug: `/Contents` object number now `5 + pageCount + i`, absolute `Tm` layout) and regenerated (5291 bytes, 2 pages), but a later check reported the Desktop file missing; user confirm needed to re-run/verify.
- Previous session tasks (see IMPLEMENTATION_*.md/COMPLETED_FEATURES.md): page-title-in-header propagation to all pages, JobApplyForm position field readonly, Reports button alignment, JBM full-window views (partly done via viewState).

## Active Files
- src/app/components/shared/ShopStatusControl.tsx
- src/app/components/shared/ShopStatusBanner.tsx
- src/app/utils/shopStatusStore.ts
- src/app/components/shared/PrintTransaction.tsx
- src/app/components/shared/PaymentDeadlineCountdown.tsx
- src/app/utils/paymentExpiry.ts
- src/app/components/shared/LegalPolicyDialog.tsx
- src/app/components/shared/CashOnPickupAcknowledgement.tsx
- src/app/components/shared/OrderPaymentSummary.tsx
- src/app/components/shared/UnifiedOrders.tsx
- src/app/components/shared/UnifiedPaymentVerification.tsx
- src/app/components/ui/priority-badge.tsx
- src/app/utils/pricingStore.ts
- src/app/utils/dataStore.ts
- src/app/utils/ordersStore.ts
- src/app/utils/paymentMethodsStore.ts
- src/app/components/shared/PaymentMethodQR.tsx
- src/app/components/admin/PaymentMethodsManagement.tsx
- src/app/components/customer/PaymentVerification.tsx
- src/app/components/customer/NewPrintRequest.tsx
- src/app/components/admin/AdminAttendance.tsx
- src/app/utils/staffRoster.ts
- src/app/components/staff/StaffTimesheet.tsx
- src/app/components/shared/StaffTimeInGate.tsx
- src/app/utils/attendanceStore.ts
- src/app/components/admin/Staff.tsx
- src/app/contexts/AuthContext.tsx
- src/app/components/customer/JobBoard.tsx
- src/app/components/admin/JobBoardManagement.tsx
- src/app/utils/jobsStore.ts
- src/app/utils/applicationsStore.ts
- src/app/components/Layout.tsx
- src/app/components/customer/CustomerDashboard.tsx
- AGENTS.md

## Architecture / Next Steps
- Update this file on every code change/feature/strategy shift.
- Payment methods = second localStorage store (alongside inventory, jobs, attendance). QR codes are uploaded data URLs in localStorage â€” large uploads bloat quota; on real backend, store uploads in Supabase Storage and keep a URL instead.
- Next: if integrating backend, move jobs/apps/portfolio/payment-methods from localStorage/in-memory to Supabase tables + storage URLs; swap preview to accept URL when no File present.
- Run npm run build to verify.
